# owner: zhenjiang.xzj
# owner group: storage
# description: this case is use for scheduling build index failed

--disable_query_log
ob1=OBI(cluster=1,proxy_num=0);
ob1.reboot;

#设置灰度切主的时间为0, 关闭自动切主，关闭自动balance
ob1.mysql alter system set resource_hard_limit = 800;
ob1.mysql alter system set merger_switch_leader_duration_time=0;
ob1.mysql alter system set enable_auto_leader_switch = false;
ob1.mysql alter system set enable_rereplication = false;
ob1.mysql alter system set enable_rebalance = false;
ob1.mysql alter system set debug_sync_timeout = '100000s';

let $ip = deploy_get_value(ob1.z1.obs0.ip);
let $port = deploy_get_value(ob1.z1.obs0.mysql_port);

ob1.create_client tenant=sys,user=root,passwd=,database=test;

create table test01(c1 int, c2 int, c3 varchar(4096), primary key(c1)) primary_zone='z1';

let $cnt=1;
while($cnt < 10)
{
  eval insert into test01 values($cnt, $cnt, 'oceanbase');
  inc $cnt;
}

eval insert into test01 values($cnt, 1, 'oceanbase');
--error 1062
create unique index index01 on test01(c2, c3);

ob1.mysql alter system major freeze;
ob1.check_version 2;

ob1.z1.obs0.mysql alter system set_tp tp_no = 17, error_code = 4013, frequency = 1; 
--error 4013
create index index02 on test01(c2, c3);