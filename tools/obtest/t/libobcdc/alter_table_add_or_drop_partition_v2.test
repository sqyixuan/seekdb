#
# owner:千溪
# owner group:liboblog
# description:
# 测试点:
#   动态增加分区，该分区插入若干条数据, 测试回拉历史场景, 保证该动态添加分区的数据都能迭代到
#   bug: 
#
##

ob1=OBI(cluster=1,init_unit_cpu=5);
ob1.reboot;

source include/liboblog/create_tenant.inc;
ob_connect(conn_obcdc_mysql, ob1.z1.obs0, root@oblog_tt, , oceanbase, , USE_MYSQL_CONNECTOR);

source include/all_ddl_type_add_or_drop_partition.inc;
