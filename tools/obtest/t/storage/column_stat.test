# owner: chenqun
# owner group: storage
# description:this case is use for column statistic test
#
--disable_query_log
ob1=OBI(cluster=2:2:2,proxy_num=0,init_unit_cpu=4);
ob1.reboot;
--enable_query_log

ob1.mysql alter system set merger_check_interval='10s';
ob1.mysql alter system set enable_rebalance = 'false';
ob1.mysql alter system set resource_hard_limit=500;
ob1.mysql alter resource pool obtestpool unit_num 2;

let $ip=deploy_get_value(ob1.z1.obs0.ip);
let $port=deploy_get_value(ob1.z1.obs0.mysql_port);
connect (conn1,$ip,admin@obtest,admin,oceanbase,$port);
connect (conn2,$ip,root@sys,,oceanbase,$port);
connection conn1;
use test;
create table test01 (pk1 int, pk2 int, pk3 int, f1 varchar(10), f2 varchar(10), primary key(pk1,pk2,pk3))progressive_merge_num=1;
let $cnt =1;
while ($cnt < 100)
{
	eval insert into test01(pk1, pk2, pk3, f1) values($cnt, $cnt, $cnt, 'abc');
	inc $cnt;
}

ob1.mysql alter system major freeze;
ob1.check_version 2;
delete from test.test01;

ob1.mysql alter system major freeze;
ob1.check_version 3;




