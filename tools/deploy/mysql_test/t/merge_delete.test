--disable_query_log
set @@session.explicit_defaults_for_timestamp=off;
--enable_query_log
# owner: dachuan.sdc
# owner group: SQL3
# description: 插入数据做合并；插入数据做合并，检查数据是否正确，挨行进行删除，检查正确行
#--disable_parsing
--disable_warnings
drop table if exists sbtest;
--enable_warnings

--real_sleep 1

CREATE TABLE sbtest (id INTEGER  NOT NULL , k integer  DEFAULT '0' NOT NULL, c char(120) DEFAULT '' NOT NULL, pad char(60) DEFAULT '' NOT NULL, PRIMARY KEY  (id) ) ;

--echo insert 100 rows to ups
--let $count=1
while($count < 101)
{ 
    --let $stmt=INSERT INTO sbtest(id, k, c, pad) VALUES($count,0,' ','qqqqqqqqqqwwwwwwwwwweeeeeeeeeerrrrrrrrrrtttttttttt')
    eval $stmt;
    inc $count; 
}

--echo merge
--source mysql_test/include/majorfreeze.inc
--source mysql_test/include/wait_daily_merge.inc

--let $count=1
while($count < 101)
{ 
    --let $stmt=select * from sbtest where id = $count
    eval $stmt;
    inc $count; 
}
--echo insert another 100 rows to ups

--let $count=101
while($count < 201)
{ 
    --let $stmt=INSERT INTO sbtest(id, k, c, pad) VALUES($count,0,' ','qqqqqqqqqqwwwwwwwwwweeeeeeeeeerrrrrrrrrrtttttttttt')
    eval $stmt;
    inc $count; 
}

--echo merge
--source mysql_test/include/majorfreeze.inc
--source mysql_test/include/wait_daily_merge.inc

--let $count=101
while($count < 201)
{ 
    --let $stmt=select * from sbtest where id = $count
    eval $stmt;
    inc $count; 
}

--echo insert another 100 rows to ups
--let $count=201
while($count < 301)
{ 
    --let $stmt=INSERT INTO sbtest(id, k, c, pad) VALUES($count,0,' ','qqqqqqqqqqwwwwwwwwwweeeeeeeeeerrrrrrrrrrtttttttttt')
    eval $stmt;
    inc $count; 
}

# insert
--let $count=1
while($count < 101)
{
   eval delete from sbtest where id = $count;
   eval select * from sbtest where id = $count;
   inc $count;
   inc $count;
   inc $count;
   inc $count;
   inc $count;
}
--let $count=101
while($count < 201)
{
   eval delete from sbtest where id = $count;
   eval select * from sbtest where id = $count;
   inc $count;
   inc $count;
   inc $count;
   inc $count;
   inc $count;
}
--let $count=201
while($count < 301)
{
   eval delete from sbtest where id = $count;
   eval select * from sbtest where id = $count;
   inc $count;
   inc $count;
   inc $count;
   inc $count;
   inc $count;
}


drop table sbtest;
