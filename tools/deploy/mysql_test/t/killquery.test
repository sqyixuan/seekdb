--disable_query_log
set @@session.explicit_defaults_for_timestamp=off;
--enable_query_log
# owner: yuchen.wyc
# owner group: SQL1
# description: test for kill query
# note: kill query的功能是终止正在执行的query，由于当前检查kill被query的检查点设置的不够，所以无法在test中模拟；
#       当前能成功执行kill query的场景执行大查询，让后进行kill操作
# bug : #6618103


--disable_warnings
drop table if exists t1,t2;
--enable_warnings

# #create table t1(c1 int primary key, b int);
# #insert into t1 values(1,1),(2,2),(3,3);

# --echo use default connection
# --replace_column 1 id 3 client 6 time 9 ms 10 idx
# #show processlist;

# #let $a=query_get_value(select id from (select * from oceanbase.__all_server_session) as tt  where command like '%select%',id,1);

# --echo a new connection named by conn1
# connect (conn1,$OBMYSQL_MS0,$OBMYSQL_USR,$OBMYSQL_PWD,test,$OBMYSQL_PORT);
# --replace_column 1 id 3 client 6 time  9 ms 10 idx
# show processlist;
# #let $b=query_get_value(select id from (select * from oceanbase.__all_server_session) as tt  where command like '%select%',id,1);
# --echo a new connection named by conn2
# connect (conn2,$OBMYSQL_MS0,$OBMYSQL_USR,$OBMYSQL_PWD,test,$OBMYSQL_PORT);

# #let $c=query_get_value(select id from (select * from oceanbase.__all_server_session) as tt  where command like '%select%',id,1);
# --replace_column 1 id 3 client 6 time  9 ms 10 idx
# show processlist;

# connection conn2;
# --replace_column 1 id 3 client 6 time  9 ms 10 idx
# show processlist;

# --echo kill query
# --disable_query_log
# eval kill global query  $b;
# --enable_query_log


# connection conn1;
# --error S70100
# select * from t1;

# connection conn1;
# begin;
# select * from t1 where c1=1 for update;

# connection conn2;
# --replace_column 1 id 3 client 6 time  9 ms 10 idx
# show processlist;

# --echo kill query
# --disable_query_log
# eval kill global query  $b;
# --enable_query_log


# --replace_column 1 id 3 client 6 time  9 ms 10 idx
# show processlist;

# connection conn1;
# --error S70100
# commit;

# connection conn2;
# --replace_column 1 id 3 client 6 time  9 ms 10 idx
# show processlist;


# --echo kill query again
# --disable_query_log
# eval kill global query  $b;
# --enable_query_log


# --replace_column 1 id 3 client 6 time  9 ms 10 idx
# show processlist;


# connection conn1;
# --error S70100
# begin;
# commit;
# begin;
# select * from t1 where c1=2 for update;
# commit;
