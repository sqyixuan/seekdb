--disable_query_log
set @@session.explicit_defaults_for_timestamp=off;
--enable_query_log
# owner: dachuan.sdc
# owner group: SQL3
# description: insert on duplicate key. 只验证插入功能，合并，验证··

--disable_warnings
drop table if exists sbtest;
--enable_warnings

#alter system set min_drop_cache_wait_time='10s' server_type=chunkserver;
--real_sleep 1

CREATE TABLE sbtest (id INTEGER  NOT NULL , k integer  DEFAULT '0' NOT NULL, c char(120) DEFAULT '' NOT NULL, pad char(60) DEFAULT '' NOT NULL, PRIMARY KEY  (id) ) ;

--echo insert 100 rows to ups
--let $count=1
while($count < 101)
{ 
    --let $stmt=INSERT INTO sbtest(id, k, c, pad) VALUES($count,0,' ','qqqqqqqqqqwwwwwwwwwweeeeeeeeeerrrrrrrrrrtttttttttt') on duplicate key update k = id;
    eval $stmt;
    inc $count; 
}

--echo merge
--source mysql_test/include/majorfreeze.inc
--source mysql_test/include/wait_daily_merge.inc

--let $count=1
while($count < 101)
{ 
    --let $stmt=select * from sbtest where id = $count
    eval $stmt;
    inc $count; 
}
--echo insert another 100 rows to ups

--let $count=101
while($count < 201)
{ 
    --let $stmt=INSERT INTO sbtest(id, k, c, pad) VALUES($count,0,' ','qqqqqqqqqqwwwwwwwwwweeeeeeeeeerrrrrrrrrrtttttttttt') on duplicate key update k = id;
    eval $stmt;
    inc $count; 
}

--echo merge
--source mysql_test/include/majorfreeze.inc
--source mysql_test/include/wait_daily_merge.inc

--let $count=101
while($count < 201)
{ 
    --let $stmt=select * from sbtest where id = $count
    eval $stmt;
    inc $count; 
}

--echo insert another 100 rows to ups
--let $count=201
while($count < 301)
{ 
    --let $stmt=INSERT INTO sbtest(id, k, c, pad) VALUES($count,0,' ','qqqqqqqqqqwwwwwwwwwweeeeeeeeeerrrrrrrrrrtttttttttt') on duplicate key update k = id;
    eval $stmt;
    inc $count; 
}

# insert
--let $count=1
while($count < 101)
{
   --error 1062
   eval insert into sbtest(id, k, c, pad) values($count,0,' ',' ');
   inc $count;
   inc $count;
   inc $count;
   inc $count;
   inc $count;
}
--let $count=101
while($count < 201)
{
   --error 1062
   eval insert into sbtest(id, k, c, pad) values($count,0,' ',' ');
   inc $count;
   inc $count;
   inc $count;
   inc $count;
   inc $count;
}
--let $count=201
while($count < 301)
{
   --error 1062
   eval insert into sbtest(id, k, c, pad) values($count,0,' ',' ');
   inc $count;
   inc $count;
   inc $count;
   inc $count;
   inc $count;
}

insert into sbtest(id, k, c, pad) values(301,0,' ',' ');



drop table sbtest;
