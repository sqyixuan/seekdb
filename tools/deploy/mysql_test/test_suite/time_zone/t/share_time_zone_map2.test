# owner: dachuan.sdc
# owner group: SQL2
# description:  test for share time_zone map
# tags: time_zone

connect (conn_admin, $OBMYSQL_MS0,admin,$OBMYSQL_PWD,oceanbase,$OBMYSQL_PORT);
connection conn_admin;

## 1. import timezone info for one tenant.
### import
let $tz_file = 0;
let $TENANT = tenant1;
eval call oceanbase.drop_tenant_force('$TENANT');
eval call oceanbase.create_mysql_tenant_mini('$TENANT');
--source mysql_test/test_suite/time_zone/t/include/import_time_zone_for_tenant.inc
connection conn_admin;
eval call oceanbase.drop_tenant_force('$TENANT');
--sleep 20
eval call oceanbase.create_mysql_tenant_mini('$TENANT');
--source mysql_test/test_suite/time_zone/t/include/import_time_zone_for_tenant.inc
### recpord memory usage
connection conn_admin;
eval call oceanbase.drop_tenant_force('$TENANT');
--sleep 30
--disable_query_log
--disable_warnings
drop table if exists test.mem_used ;
drop table if exists test.mem_used_new;
create table test.mem_used  as select tenant_id, label, ctx_name, mod_type, mod_id, mod_name, used, count
  from __all_virtual_memory_info
  where label like '%TZ%' and SVR_IP=HOST_IP() and SVR_PORT=RPC_PORT() order by tenant_id, label;
create table test.mem_used_new like test.mem_used ;
--enable_query_log
--enable_warnings



### 4.5 same number of timezone, one timezone has more transition type
let $tz_file = 5;
let $TENANT = tenant7;
eval call oceanbase.drop_tenant_force('$TENANT');
eval call oceanbase.create_mysql_tenant_mini('$TENANT');
--source mysql_test/test_suite/time_zone/t/include/import_time_zone_for_tenant.inc
connection conn_admin;
--sleep 20
delete from test.mem_used_new;
insert into test.mem_used_new select tenant_id, label, ctx_name, mod_type, mod_id, mod_name, used, count from __all_virtual_memory_info
  where label like '%TZ%' and SVR_IP=HOST_IP() and SVR_PORT=RPC_PORT() order by tenant_id, label;
--echo ## expect allocate huge memory.
select a.tenant_id, a.label, a.ctx_name, a.mod_type, a.mod_id, a.mod_name, a.used - b.used, a.count - b.count
  from test.mem_used_new a, test.mem_used  b where a.tenant_id = b.tenant_id and a.label = b.label and a.mod_id = b.mod_id order by tenant_id, label;

connect (conn7, $OBMYSQL_MS0,root@tenant7, ,oceanbase,$OBMYSQL_PORT);
connection conn7;
set time_zone = 'Africa/Accra';
set time_zone = 'Africa/Abidjan';
connection conn_admin;
eval call oceanbase.drop_tenant_force('$TENANT');
--sleep 10


### 4.6 same number of timezone, one timezone has less transition type
let $tz_file = 6;
let $TENANT = tenant8;
eval call oceanbase.drop_tenant_force('$TENANT');
eval call oceanbase.create_mysql_tenant_mini('$TENANT');
--source mysql_test/test_suite/time_zone/t/include/import_time_zone_for_tenant.inc
connection conn_admin;
--sleep 20
delete from test.mem_used_new;
insert into test.mem_used_new select tenant_id, label, ctx_name, mod_type, mod_id, mod_name, used, count from __all_virtual_memory_info
  where label like '%TZ%' and SVR_IP=HOST_IP() and SVR_PORT=RPC_PORT() order by tenant_id, label;
--echo ## expect allocate huge memory.
select a.tenant_id, a.label, a.ctx_name, a.mod_type, a.mod_id, a.mod_name, a.used - b.used, a.count - b.count
  from test.mem_used_new a, test.mem_used  b where a.tenant_id = b.tenant_id and a.label = b.label and a.mod_id = b.mod_id order by tenant_id, label;

connect (conn8, $OBMYSQL_MS0,root@tenant8,,oceanbase,$OBMYSQL_PORT);
connection conn8;
set time_zone = 'Africa/Accra';
set time_zone = 'Africa/Abidjan';
connection conn_admin;
eval call oceanbase.drop_tenant_force('$TENANT');
--sleep 10


### 4.7 same number of timezone, one timezone has different transition name
let $tz_file = 7;
let $TENANT = tenant9;
eval call oceanbase.drop_tenant_force('$TENANT');
eval call oceanbase.create_mysql_tenant_mini('$TENANT');
--source mysql_test/test_suite/time_zone/t/include/import_time_zone_for_tenant.inc
connection conn_admin;
--sleep 20
delete from test.mem_used_new;
insert into test.mem_used_new select tenant_id, label, ctx_name, mod_type, mod_id, mod_name, used, count from __all_virtual_memory_info
  where label like '%TZ%' and SVR_IP=HOST_IP() and SVR_PORT=RPC_PORT() order by tenant_id, label;
--echo ## expect allocate huge memory.
select a.tenant_id, a.label, a.ctx_name, a.mod_type, a.mod_id, a.mod_name, a.used - b.used, a.count - b.count
  from test.mem_used_new a, test.mem_used  b where a.tenant_id = b.tenant_id and a.label = b.label and a.mod_id = b.mod_id order by tenant_id, label;

connect (conn9, $OBMYSQL_MS0,root@tenant9,,oceanbase,$OBMYSQL_PORT);
connection conn9;
set time_zone = 'Africa/Accra';
set time_zone = 'Africa/Abidjan';
connection conn_admin;
eval call oceanbase.drop_tenant_force('$TENANT');
--sleep 10


### 4.8 same number of timezone, one timezone has different transition time
let $tz_file = 8;
let $TENANT = tenant10;
eval call oceanbase.drop_tenant_force('$TENANT');
eval call oceanbase.create_mysql_tenant_mini('$TENANT');
--source mysql_test/test_suite/time_zone/t/include/import_time_zone_for_tenant.inc
connection conn_admin;
--sleep 20
delete from test.mem_used_new;
insert into test.mem_used_new select tenant_id, label, ctx_name, mod_type, mod_id, mod_name, used, count from __all_virtual_memory_info
  where label like '%TZ%' and SVR_IP=HOST_IP() and SVR_PORT=RPC_PORT() order by tenant_id, label;
--echo ## expect allocate huge memory.
select a.tenant_id, a.label, a.ctx_name, a.mod_type, a.mod_id, a.mod_name, a.used - b.used, a.count - b.count
  from test.mem_used_new a, test.mem_used  b where a.tenant_id = b.tenant_id and a.label = b.label and a.mod_id = b.mod_id order by tenant_id, label;

connect (conn10, $OBMYSQL_MS0,root@tenant10,,oceanbase,$OBMYSQL_PORT);
connection conn10;
set time_zone = 'Africa/Accra';
set time_zone = 'Africa/Abidjan';
connection conn_admin;
eval call oceanbase.drop_tenant_force('$TENANT');
--sleep 10


### 4.9 same number of timezone, one timezone has different transition type
let $tz_file = 9;
let $TENANT = tenant11;
eval call oceanbase.drop_tenant_force('$TENANT');
eval call oceanbase.create_mysql_tenant_mini('$TENANT');
--source mysql_test/test_suite/time_zone/t/include/import_time_zone_for_tenant.inc
connection conn_admin;
--sleep 20
delete from test.mem_used_new;
insert into test.mem_used_new select tenant_id, label, ctx_name, mod_type, mod_id, mod_name, used, count from __all_virtual_memory_info
  where label like '%TZ%' and SVR_IP=HOST_IP() and SVR_PORT=RPC_PORT() order by tenant_id, label;
--echo ## expect allocate huge memory.
select a.tenant_id, a.label, a.ctx_name, a.mod_type, a.mod_id, a.mod_name, a.used - b.used, a.count - b.count
  from test.mem_used_new a, test.mem_used  b where a.tenant_id = b.tenant_id and a.label = b.label and a.mod_id = b.mod_id order by tenant_id, label;

connect (conn11, $OBMYSQL_MS0,root@tenant11,,oceanbase,$OBMYSQL_PORT);
connection conn11;
set time_zone = 'Africa/Accra';
set time_zone = 'Africa/Abidjan';
connection conn_admin;
eval call oceanbase.drop_tenant_force('$TENANT');
--sleep 10


### 4.10 same number of timezone, one timezone has different transition offset
let $tz_file = 10;
let $TENANT = tenant12;
eval call oceanbase.drop_tenant_force('$TENANT');
eval call oceanbase.create_mysql_tenant_mini('$TENANT');
--source mysql_test/test_suite/time_zone/t/include/import_time_zone_for_tenant.inc
connection conn_admin;
--sleep 20
delete from test.mem_used_new;
insert into test.mem_used_new select tenant_id, label, ctx_name, mod_type, mod_id, mod_name, used, count from __all_virtual_memory_info
  where label like '%TZ%' and SVR_IP=HOST_IP() and SVR_PORT=RPC_PORT() order by tenant_id, label;
--echo ## expect allocate huge memory.
select a.tenant_id, a.label, a.ctx_name, a.mod_type, a.mod_id, a.mod_name, a.used - b.used, a.count - b.count
  from test.mem_used_new a, test.mem_used  b where a.tenant_id = b.tenant_id and a.label = b.label and a.mod_id = b.mod_id order by tenant_id, label;

connect (conn12, $OBMYSQL_MS0,root@tenant12,,oceanbase,$OBMYSQL_PORT);
connection conn12;
set time_zone = 'Africa/Accra';
set time_zone = 'Africa/Abidjan';
connection conn_admin;
eval call oceanbase.drop_tenant_force('$TENANT');
--sleep 10



## 5. ## test recycle memory of dropped tenant.
connection conn_admin;
--disable_query_log
--sleep 30
delete from test.mem_used_new;
insert into test.mem_used_new select tenant_id, label, ctx_name, mod_type, mod_id, mod_name, used, count from __all_virtual_memory_info
  where label like '%TZ%' and SVR_IP=HOST_IP() and SVR_PORT=RPC_PORT() order by tenant_id, label;
--echo ## expect allocate zero memory.
select a.tenant_id, a.label, a.ctx_name, a.mod_type, a.mod_id, a.mod_name, a.used - b.used, a.count - b.count
  from test.mem_used_new a, test.mem_used  b where a.tenant_id = b.tenant_id and a.label = b.label and a.mod_id = b.mod_id order by tenant_id, label;
drop table test.mem_used ;
drop table test.mem_used_new;
--enable_query_log


