--disable_query_log
set @@session.explicit_defaults_for_timestamp=off;
--enable_query_log
# owner: yibo.tyf
# owner group: SQL1
# bug:

--disable_warnings
drop table if exists Z1CASE;
--enable_warnings
--result_format 4
CREATE TABLE Z1CASE (T1 CHAR(20) CHARACTER SET 'binary' COLLATE 'binary', T2 VARCHAR(10)) PARTITION BY KEY(T1) PARTITIONS 3 ;
INSERT INTO Z1CASE VALUES(11,11);
insert into Z1CASE(T1,T2) select 11,11 from dual;
select * from Z1CASE partition(p2);

drop table Z1CASE;