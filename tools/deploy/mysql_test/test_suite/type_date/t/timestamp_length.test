--disable_query_log
set @@session.explicit_defaults_for_timestamp=off;
--enable_query_log
# owner: dachuan.sdc
# owner group: SQL1
# tags: datatype

select length(utc_timestamp());
select length(utc_timestamp(0));
select length(utc_timestamp(1));
select length(utc_timestamp(2));
select length(utc_timestamp(3));
select length(utc_timestamp(4));
select length(utc_timestamp(5));
select length(utc_timestamp(6));
select length(current_timestamp());
select length(current_timestamp(0));
select length(current_timestamp(1));
select length(current_timestamp(2));
select length(current_timestamp(3));
select length(current_timestamp(4));
select length(current_timestamp(5));
select length(current_timestamp(6));
select length(current_time());
select length(current_time(0));
select length(current_time(1));
select length(current_time(2));
select length(current_time(3));
select length(current_time(4));
select length(current_time(5));
select length(current_time(6));


