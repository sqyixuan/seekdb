# owner: jingfeng.jf
# owner group: SQL2
# tags: sql audit request id order
--result_format 4

connect (con, $OBMYSQL_MS0, admin, $OBMYSQL_PWD, test, $OBMYSQL_PORT);
connection con;

select IFNULL(max(t1.request_id)-min(t1.request_id)+1,0) = count(1) from (select request_id from oceanbase.v$ob_sql_audit order by request_id limit 100) t1;
