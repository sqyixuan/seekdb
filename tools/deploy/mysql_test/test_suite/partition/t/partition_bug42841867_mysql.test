# owner: yibo.tyf
#tags: optimizer
# owner group: SQL1
# tags: partition

--disable_query_log
set @@session.explicit_defaults_for_timestamp=off;
--enable_query_log

--disable_warnings
drop table if exists obschema_t_zD6lexr0W;
--enable_warnings

CREATE TABLE obschema_t_zD6lexr0W(obschema_c_0_LiIY bigint(255) NOT NULL DEFAULT 3, PRIMARY key(obschema_c_0_LiIY))
 partition BY range((-obschema_c_0_LiIY))
 (PARTITION p0 VALUES LESS THAN (447));

drop table obschema_t_zD6lexr0W;