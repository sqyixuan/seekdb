# owner: lihongqin.lhq
# owner group: storage
# description: this case is use for testing create index on tables with special name
#

--disable_warnings
drop table if exists t1;
drop table if exists `t``1`;
--enable_warnings

--echo
--echo # special partition name
create table t1(c1 int primary key) partition by range(c1) (partition `1` values less than (10), partition `2` values less than (20), partition `p``3` values less than (30));
insert into t1 values (1), (15);
create index i1 on t1(c1) local;
select * from t1 order by c1;
select /*+index(t1 i1)*/ * from t1 order by c1;
drop table t1;

#TODO:
#--echo
#--echo # special column name
#create table `t``1`(`c``1` int primary key, `22` int, `33` int) partition by range(`c``1`) (partition `1` values less than (10), partition `2` values less than (20), partition `p``3` values less than (30));
#insert into `t``1` values (1, 2, 3), (15, 16, 17);
#create index i1 on `t``1`(`c``1`, `22`) local;
#select * from `t``1` order by `c``1`;
#select /*+index(`t``1` i1)*/ * from `t``1` order by `c``1`;

