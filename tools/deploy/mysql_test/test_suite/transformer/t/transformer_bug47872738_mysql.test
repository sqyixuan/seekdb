#owner: wuyuming.wym
#owner group: sql1
# tags: optimizer

--disable_warnings
drop table if exists t_kms_a_key_app;

CREATE TABLE t_kms_a_key_app (
  id varchar(36) PRIMARY KEY
);

--enable_warnings
--explain_protocol 2
--result_format 4

#bug:
select /*+use_concat*/ ref_0.id from t_kms_a_key_app ref_0 where (select count(1) from dual) >= (select 1 from dual limit 1) or (select 1 from dual limit 1);

drop table t_kms_a_key_app;