# owner: yibo.tyf
# owner group: SQL1
# tags: optimizer
# issue: 

--disable_warnings
drop table if exists sqlgen66___tbl_2;
--enable_warnings

CREATE TABLE sqlgen66___tbl_2 (
  `col_2` enum('Alice','Bob','Charlie','David') NOT NULL DEFAULT 'Charlie',
  `col_3` int,
  PRIMARY KEY (col_2)
) WITH COLUMN GROUP (all columns, each column);

explain select /*+no_rewrite*/ col_3 from sqlgen66___tbl_2 order by random() limit 1;
select /*+no_rewrite*/ col_3 from sqlgen66___tbl_2 order by random() limit 1;

drop table sqlgen66___tbl_2;

CREATE TABLE sqlgen66___tbl_2 (
  `col_2` enum('Alice','Bob','Charlie','David') NOT NULL DEFAULT 'Charlie' primary key ,
  `col_3` int,
  col_4 varchar(100),
  col_5 varchar(100),
  col_6 varchar(100),
  index i1 (col_3)
) ;

explain select /*+use_late_materialization index(sqlgen66___tbl_2 i1)*/ * from sqlgen66___tbl_2 order by col_2 limit 1;
select /*+use_late_materialization index(sqlgen66___tbl_2 i1)*/ * from sqlgen66___tbl_2 order by col_2 limit 1;

drop table sqlgen66___tbl_2;
