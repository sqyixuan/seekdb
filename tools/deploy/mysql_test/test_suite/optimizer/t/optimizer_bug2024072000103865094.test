# owner: guohanyun.ghy
# owner group: SQL1
# tags: optimizer

--disable_warnings
drop table if exists table100_hash_pk_parts_4;
--enable_warnings

CREATE TABLE `table100_hash_pk_parts_4` (
  `col_bigint` bigint(20) DEFAULT NULL,
  `col_double` double DEFAULT NULL,
  `col_decimal_20_0` decimal(20,0) DEFAULT NULL,
  `col_varchar_20` varchar(20) DEFAULT NULL,
  `pk` int(11) DEFAULT NULL,
  KEY `in_expr_idx16` (`col_double`, `col_decimal_20_0`) BLOCK_SIZE 16384 LOCAL
) DEFAULT CHARSET = utf8mb4 ROW_FORMAT = DYNAMIC COMPRESSION = 'zstd_1.3.8' REPLICA_NUM = 1 BLOCK_SIZE = 16384 USE_BLOOM_FILTER = FALSE TABLET_SIZE = 134217728 PCTFREE = 0
 partition by hash(pk)
(partition `p0`,
partition `p1`,
partition `p2`,
partition `p3`);

insert into table100_hash_pk_parts_4 values (1,1,1,'a1',1);
insert into table100_hash_pk_parts_4 values (1,1,1,'a7',1);
insert into table100_hash_pk_parts_4 values (1,1,1,'a3',1);
insert into table100_hash_pk_parts_4 values (1,1,1,'x',1);
insert into table100_hash_pk_parts_4 values (1,1,1,'NULL',1);

explain SELECT 
/*+     parallel(2) leading(view4(view3 table100_hash_pk_parts_4))                                         
*/                                                                                                                       
 1 
FROM table100_hash_pk_parts_4
 WHERE (col_varchar_20,col_varchar_20) IN (values row('a7','a7'),row('a3','a4')) 
 AND col_varchar_20 NOT IN (values row(NULL),row('xytfcwwi'),row('x')) ;

SELECT 
/*+     parallel(2) leading(view4(view3 table100_hash_pk_parts_4))                                         
*/                                                                                                                       
 1 
FROM table100_hash_pk_parts_4
 WHERE (col_varchar_20,col_varchar_20) IN (values row('a7','a7'),row('a3','a4')) 
 AND col_varchar_20 NOT IN (values row(NULL),row('xytfcwwi'),row('x')) ;

explain SELECT 
/*+     parallel(2) leading(view4(view3 table100_hash_pk_parts_4))                                         
*/                                                                                                                       
 1 
FROM table100_hash_pk_parts_4
 WHERE (col_varchar_20,col_varchar_20) IN (values row('a7','a7'),row('a3','a4')) 
 AND col_varchar_20 NOT IN (values row('xytfcwwi'),row('x')) ;

SELECT 
/*+     parallel(2) leading(view4(view3 table100_hash_pk_parts_4))                                         
*/                                                                                                                       
 1 
FROM table100_hash_pk_parts_4
 WHERE (col_varchar_20,col_varchar_20) IN (values row('a7','a7'),row('a3','a4')) 
 AND col_varchar_20 NOT IN (values row('xytfcwwi'),row('x')) ;

drop table table100_hash_pk_parts_4;