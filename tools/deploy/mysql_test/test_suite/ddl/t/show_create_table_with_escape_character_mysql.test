# owner : dachuan.sdc
# owner group : SQL1
# description :  show create table test
# tags: ddl 

--result_format 4
--disable_query_log
set @@session.explicit_defaults_for_timestamp=off;
--enable_query_log

--disable_result_log
drop table if exists ``````;
drop table if exists ````;
--enable_result_log

create table ```` (```` int primary key, `````` int, index ````````(``````));
create table `````` (```` int primary key, `````` int, constraint ```````` foreign key (``````) references ````(````));
--source mysql_test/include/show_create_table_old_version_replica2.inc
show create table ````;
--source mysql_test/include/show_create_table_old_version_replica2.inc
show create table ``````;

# clean up
drop table ``````;
drop table ````;