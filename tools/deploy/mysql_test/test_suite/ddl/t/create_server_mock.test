# owner: sanquan.qz
# owner group: sql1
# tag: ddl


connect (sys_conn,$OBMYSQL_MS0,admin,$OBMYSQL_PWD,test,$OBMYSQL_PORT);
connection sys_conn;

create server xx foreign data wrapper ab options (user 'remote', host '111');
alter server xx options (user 'remote', host '111', database 'xx', owner 'yy', password 'zz', socket 'aa', port 234);
drop server xx;
drop server if exists xx;

disconnect sys_conn;
