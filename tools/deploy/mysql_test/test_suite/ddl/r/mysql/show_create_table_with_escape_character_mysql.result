result_format: 4
drop table if exists ``````;
drop table if exists ````;
create table ```` (```` int primary key, `````` int, index ````````(``````));
create table `````` (```` int primary key, `````` int, constraint ```````` foreign key (``````) references ````(````));
show create table ````;
Table	Create Table
`	CREATE TABLE ```` (
  ```` int(11) NOT NULL,
  `````` int(11) DEFAULT NULL,
  PRIMARY KEY (````),
  KEY ```````` (``````) BLOCK_SIZE 16384 LOCAL
) ORGANIZATION INDEX DEFAULT CHARSET = utf8mb4 COMPRESSION = 'lz4_1.0' REPLICA_NUM = NUM BLOCK_SIZE = SIZE USE_BLOOM_FILTER = FALSE ENABLE_MACRO_BLOCK_BLOOM_FILTER = FALSE TABLET_SIZE = SIZE PCTFREE = 10
show create table ``````;
Table	Create Table
``	CREATE TABLE `````` (
  ```` int(11) NOT NULL,
  `````` int(11) DEFAULT NULL,
  PRIMARY KEY (````),
  CONSTRAINT ```````` FOREIGN KEY (``````) REFERENCES `test`.````(````) ON UPDATE RESTRICT ON DELETE RESTRICT 
) ORGANIZATION INDEX DEFAULT CHARSET = utf8mb4 COMPRESSION = 'lz4_1.0' REPLICA_NUM = NUM BLOCK_SIZE = SIZE USE_BLOOM_FILTER = FALSE ENABLE_MACRO_BLOCK_BLOOM_FILTER = FALSE TABLET_SIZE = SIZE PCTFREE = 10

drop table ``````;
drop table ````;
