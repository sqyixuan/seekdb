#owner: shouju.zyp
#owner group: RS
--disable_query_log
set @@session.explicit_defaults_for_timestamp=off;
--enable_query_log

##over all test
connect (obsys1,$OBMYSQL_MS0,admin,$OBMYSQL_PWD,test,$OBMYSQL_PORT);
connection obsys1;
use oceanbase;

set @@ob_query_timeout = 100000000;
set @@ob_log_level = 'WARN';

source check_archive_progress.sql;
call check_not_archive_ls_count(1002);
