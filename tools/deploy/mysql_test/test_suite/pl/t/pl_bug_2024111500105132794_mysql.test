#owner: heyongyi.hyy
#owner group: PL/SQL
#tags: pl
#description: test pl bug

--result_format 4

--disable_warnings ONCE
drop user if exists tester;
create user tester;

--disable_warnings ONCE
drop user if exists tester2;
create user tester2;

select @@lower_case_table_names;

--disable_warnings ONCE
drop database if exists db;

create database `DB`;

--error 1007
create database `db`;

--disable_warnings ONCE
drop function if exists `DB`.`f_Test`;

--replace_column 1 ########################## 2 ########################## 3 ###### 4 ######
select * from oceanbase.__all_routine_privilege where upper(cast(database_name as char)) = 'DB' and upper(cast(routine_name as char)) = 'F_TEST' order by database_name, routine_name;

create function `DB`.`f_Test`() returns int return 1;

--error 1304
create function `db`.`f_test`() returns int return 1;

grant execute on function `DB`.`f_test` to tester;

grant execute on function `db`.`f_test` to tester2;

--replace_column 1 ########################## 2 ########################## 3 ###### 4 ######
select * from oceanbase.__all_routine_privilege where upper(cast(database_name as char)) = 'DB' and upper(cast(routine_name as char)) = 'F_TEST' order by database_name, routine_name;

grant execute on function `DB`.`f_test` to tester;

--replace_column 1 ########################## 2 ########################## 3 ###### 4 ######
select * from oceanbase.__all_routine_privilege where upper(cast(database_name as char)) = 'DB' and upper(cast(routine_name as char)) = 'F_TEST' order by database_name, routine_name;

grant execute on function `db`.`f_test` to tester;

--replace_column 1 ########################## 2 ########################## 3 ###### 4 ######
select * from oceanbase.__all_routine_privilege where upper(cast(database_name as char)) = 'DB' and upper(cast(routine_name as char)) = 'F_TEST' order by database_name, routine_name;

grant execute on function `DB`.`f_Test` to tester;

--replace_column 1 ########################## 2 ########################## 3 ###### 4 ######
select * from oceanbase.__all_routine_privilege where upper(cast(database_name as char)) = 'DB' and upper(cast(routine_name as char)) = 'F_TEST' order by database_name, routine_name;

grant execute on function `db`.`f_Test` to tester;

--replace_column 1 ########################## 2 ########################## 3 ###### 4 ######
select * from oceanbase.__all_routine_privilege where upper(cast(database_name as char)) = 'DB' and upper(cast(routine_name as char)) = 'F_TEST' order by database_name, routine_name;

drop function db.f_test;

--replace_column 1 ########################## 2 ########################## 3 ###### 4 ######
select * from oceanbase.__all_routine_privilege where upper(cast(database_name as char)) = 'DB' and upper(cast(routine_name as char)) = 'F_TEST' order by database_name, routine_name;

drop user tester;

drop user tester2;

drop database db;

connect (obsys,$OBMYSQL_MS0,admin,$OBMYSQL_PWD,test,$OBMYSQL_PORT);
connection obsys;
 
--disable_warnings
--enable_warnings
 
#create resource pool bug_2024111500105132794_pool unit='1c2g',unit_num=1;
#create tenant bug_2024111500105132794_tenant resource_pool_list=('bug_2024111500105132794_pool') set ob_tcp_invited_nodes='%',ob_compatibility_mode='mysql',lower_case_table_names=0;
--source mysql_test/include/check_tenant_sync.inc

disconnect obsys;

connection default;
 
let $__tenant_name__ = 'sys';
--source mysql_test/include/get_tenant_server.inc
connect (my_conn, $TE_SERVER_IP, root,,*NO-ONE*, $TE_SERVER_PORT);
connection my_conn;

select @@lower_case_table_names;

create database db;
--error 1007
create database DB;

create function `db`.`f_Test`() returns int return 1;

--error 1304
create function `db`.`f_test`() returns int return 1;

#create function `DB`.`f_Test`() returns int return 1;

--error 1304
create function `DB`.`f_test`() returns int return 1;

create user tester;

grant execute on function `DB`.`f_test` to tester;
grant execute on function `db`.`f_test` to tester;

--replace_column 1 ########################## 2 ########################## 3 ###### 4 ######
select * from oceanbase.__all_routine_privilege order by database_name, routine_name;

drop function `db`.`f_test`;

--replace_column 1 ########################## 2 ########################## 3 ###### 4 ######
select * from oceanbase.__all_routine_privilege order by database_name, routine_name;

#drop function `DB`.`f_test`;
--replace_column 1 ########################## 2 ########################## 3 ###### 4 ######
select * from oceanbase.__all_routine_privilege;

disconnect my_conn;

connect (obsys,$OBMYSQL_MS0,admin,$OBMYSQL_PWD,test,$OBMYSQL_PORT);
connection obsys;

#drop tenant bug_2024111500105132794_tenant;
#DROP RESOURCE POOL bug_2024111500105132794_pool;

disconnect obsys;
 
connection default;
