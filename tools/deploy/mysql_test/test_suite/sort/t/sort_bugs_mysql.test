# owner: luofan.zp
# owner group: SQL2
# tags: sort

################################################# bug:48291634 ####################################

--disable_warnings
drop table if exists t_ps_limit;
--enable_warnings
--result_format 4

create table t_ps_limit(a int, b int);
insert into t_ps_limit values(1, 1); 

prepare stmt from 'select a, b from t_ps_limit order by a limit ?';
--error 0,4016
execute stmt using @ivar;

set @ivar = 1;
execute stmt using @ivar;

--disable_warnings
drop table if exists t_ps_limit;
--enable_warnings

