# owner: zhenling.zzg
# owner group: SQL1
# tags: optimizer

--result_format 1
--explain_protocol 2

--source cost_model_test_begin.inc

--disable_warnings

drop table if exists t1_hash_group;

--enable_warnings

create table t1_hash_group(
  pk int primary key,
  c1 int,
  c2 number,
  c3 varchar(10),
  c4 json,
  c5 text);

call dbms_stats.set_table_stats(
  NULL,
  't1_hash_group',
  numrows => 1000000,
  avgrlen => 100,
  nummicroblks => 6000
  );

explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ count(pk) from t1_hash_group group by c1;
explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ sum(pk) from t1_hash_group group by c1;
explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ min(pk) from t1_hash_group group by c1;
explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ max(pk) from t1_hash_group group by c1;

explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ count(pk) from t1_hash_group group by c3;
explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ sum(pk) from t1_hash_group group by c3;
explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ min(pk) from t1_hash_group group by c3;
explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ max(pk) from t1_hash_group group by c3;

explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ count(pk) from t1_hash_group group by c4;
explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ sum(pk) from t1_hash_group group by c4;
explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ min(pk) from t1_hash_group group by c4;
explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ max(pk) from t1_hash_group group by c4;

explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ count(pk) from t1_hash_group group by c1,c3,c5;
explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ sum(pk) from t1_hash_group group by c1,c3,c5;
explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ min(pk) from t1_hash_group group by c1,c3,c5;
explain select /*+USE_HASH_AGGREGATION(@"SEL$1")*/ max(pk) from t1_hash_group group by c1,c3,c5;

--source cost_model_test_end.inc
