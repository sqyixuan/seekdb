--disable_query_log
set @@session.explicit_defaults_for_timestamp=off;
--enable_query_log
# owner: beity.lhy
# owner group: storage
# description: test for creating a index with zero length string column.

--disable_warnings
drop table if exists beity_t1;
--enable_warnings

--error 1167
create table beity_t1(a varchar(0), b varchar(10), index idx(a, b));
--error 1167
create table beity_t1(a varchar(0), b varchar(10), index idx(b, a));

create table beity_t1(a varchar(0), b varchar(10));

--error 1167
create index idx on beity_t1(a, b);

--error 1167
create index idx on beity_t1(b, a);

drop table beity_t1;