set @@recyclebin = off;
drop table if exists test;
create table test (c1 int primary key, c2 int, c3 int);
insert into test values (1,1,3);
insert into test values (2,2,2);
insert into test values (3,2,1);
insert into test values (4,3,2);
insert into test values (5,3,6);
insert into test values (6,3,6);
insert into test values (7,4,2);
insert into test values (8,4,1);
insert into test values (9,4,1);
select *, 1 + 2 * sum(sum(c3)) over(partition by c2 order by c3 rows between unbounded preceding and unbounded following) from test;
c1	c2	c3	1 + 2 * sum(sum(c3)) over(partition by c2 order by c3 rows between unbounded preceding and unbounded following)
1	1	3	49
select *, sum(sum(sum(c3))) over() from test;
ERROR HY000: Invalid use of group function
select *, sum(sum(c3)) from test;
ERROR HY000: Invalid use of group function
select *, avg(sum(c3)) over() from test;
c1	c2	c3	avg(sum(c3)) over()
1	1	3	24.0000
select *, avg(sum(sum(c3))) over() from test;
ERROR HY000: Invalid use of group function
select *, group_concat(sum(c3)) over() from test;
c1	c2	c3	group_concat(sum(c3)) over()
1	1	3	24
select *, group_concat(sum(sum(c3))) over() from test;
ERROR HY000: Invalid use of group function
select count(sum(sum(c3))) over() from test;
ERROR HY000: Invalid use of group function
select count(sum(c3)) over() from test;
count(sum(c3)) over()
1
select count(sum(c3)) from test;
ERROR HY000: Invalid use of group function
select sum(sum(sum(c3))) over() from test;
ERROR HY000: Invalid use of group function
select sum(sum(c3)) over() from test;
sum(sum(c3)) over()
24
select sum(sum(c3)) from test;
ERROR HY000: Invalid use of group function
select avg(sum(sum(c3))) over() from test;
ERROR HY000: Invalid use of group function
select avg(sum(c3)) over() from test;
avg(sum(c3)) over()
24.0000
select avg(sum(c3)) from test;
ERROR HY000: Invalid use of group function
select min(sum(sum(c3))) over() from test;
ERROR HY000: Invalid use of group function
select min(sum(c3)) over() from test;
min(sum(c3)) over()
24
select min(sum(c3)) from test;
ERROR HY000: Invalid use of group function
select max(sum(sum(c3))) over() from test;
ERROR HY000: Invalid use of group function
select max(sum(c3)) over() from test;
max(sum(c3)) over()
24
select max(sum(c3)) from test;
ERROR HY000: Invalid use of group function
select group_concat(sum(sum(c3))) over() from test;
ERROR HY000: Invalid use of group function
select group_concat(sum(c3)) over() from test;
group_concat(sum(c3)) over()
24
select group_concat(sum(c3)) from test;
ERROR HY000: Invalid use of group function
