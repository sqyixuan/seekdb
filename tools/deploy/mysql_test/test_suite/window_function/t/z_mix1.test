# owner: zongmei.zzm
# owner group: sql1
# description:
# test window selection
--disable_abort_on_error
--disable_warnings
set @@recyclebin = off;
drop table if exists test;
--enable_warnings

--source schema.inc

select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c2 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c2 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c2 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c2 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c2 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c2 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c1 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c1 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c1 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c1 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c1 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c1 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c2,c3 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c2,c3 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c3 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c3 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c3 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c3 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c3 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c3 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c3,c2 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c3,c2 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c3,c2 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c3,c2 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c3 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c3 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c3 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c3 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c3 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c3 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between current row and current row) r0, avg(c3) over (partition by c1 order by c1 range between current row and current row) r1, max(c3) over (partition by c1 order by c1 range between current row and current row) r2, min(c3) over (partition by c1 order by c1 range between current row and current row) r3, avg(c1) over (partition by c1 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c1 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c1 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range 1 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range 1 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 range 1 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 range 1 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c2 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c2 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c2 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c2 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c2 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c2 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c2,c1 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c2,c1 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c2,c1 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c2,c1 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c1,c3 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c1,c3 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c1,c3 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c1,c3 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c1 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c1 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c1 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c1 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c1 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c1 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c3,c1 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c3,c1 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c3,c1 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c3,c1 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c3,c1 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c3,c1 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between current row and current row) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between current row and current row) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c3 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c2,c3 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c2,c3 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c3 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c3 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c3 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c3 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c3 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 rows c2 preceding) r1, max(c3) over (partition by c1,c2 order by c1 rows c2 preceding) r2, min(c3) over (partition by c1,c2 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c2 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c2 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c2 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c2 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c2 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c2 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c2 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c2 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c2 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c2 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c2 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c2 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c1,c2 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c1,c2 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c1,c2 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c1,c2 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range c2 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range c2 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 range c2 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 range c2 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c2 order by c1 range unbounded preceding) r1, max(c3) over (partition by c2 order by c1 range unbounded preceding) r2, min(c3) over (partition by c2 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c2 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c2 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c2 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c2 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c2 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c2 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c2 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c2 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c2 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c3 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c3 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c1,c3 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c1,c3 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows current row) r0, avg(c3) over (partition by c2 order by c1 rows current row) r1, max(c3) over (partition by c2 order by c1 rows current row) r2, min(c3) over (partition by c2 order by c1 rows current row) r3, avg(c1) over (partition by c2 order by c1 rows current row) r4, first_value(c1) over (partition by c2 order by c1 rows current row) r5, last_value(c1) over (partition by c2 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 rows c2 preceding) r1, max(c3) over (partition by c3,c1 order by c1 rows c2 preceding) r2, min(c3) over (partition by c3,c1 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c1,c2 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c1,c2 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c2 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c2 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c2 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c2 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c2 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c2 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c1,c2 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c1,c2 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows current row) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows current row) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows current row) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows current row) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows current row) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows current row) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c1 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c1 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c1 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c1 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c1 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c1 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c2 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c2 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c2 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c2 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c2 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c2 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between current row and current row) r0, avg(c3) over (partition by c3 order by c1 range between current row and current row) r1, max(c3) over (partition by c3 order by c1 range between current row and current row) r2, min(c3) over (partition by c3 order by c1 range between current row and current row) r3, avg(c1) over (partition by c3 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c3 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c3 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range current row) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range current row) r1, max(c3) over (partition by c1,c2,c3 order by c1 range current row) r2, min(c3) over (partition by c1,c2,c3 order by c1 range current row) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range current row) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range current row) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c3 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c3 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c3 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c3 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c3 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c3 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c3 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c3 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c3 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c3 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c3 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c3 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c3 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c3 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c3 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c3 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c3 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c3 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c1,c3 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c1,c3 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c1,c3 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c1,c3 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c2,c1 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c2,c1 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c2,c1 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c2,c1 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c2,c1 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c2,c1 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c2,c1 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c2,c1 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c2,c1 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c2,c1 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c1,c2 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c1,c2 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c1,c2 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c1,c2 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c1,c2 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c1,c2 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c1,c2 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c1,c2 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c1,c2 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c1,c2 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c1,c3 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c1,c3 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c1 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c1 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c1 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c1 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c1 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c1 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c1 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c1 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c1 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c1 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c2 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c2 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c2 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c2 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c2 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c2 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between current row and current row) r0, avg(c3) over (partition by c3,c1 order by c1 range between current row and current row) r1, max(c3) over (partition by c3,c1 order by c1 range between current row and current row) r2, min(c3) over (partition by c3,c1 order by c1 range between current row and current row) r3, avg(c1) over (partition by c3,c1 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c3,c1 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c3,c1 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c3,c2 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c3,c2 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c3 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c3 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c3 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c3 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c3 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c3 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 rows c2 preceding) r1, max(c3) over (partition by c3,c2 order by c1 rows c2 preceding) r2, min(c3) over (partition by c3,c2 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c2,c3 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c2,c3 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c2,c3 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c2,c3 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c3 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c3 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c3 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c3 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c3 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c3 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c2 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c2 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c2 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c2 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c2 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c2 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c3,c1 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c3,c1 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c3,c1 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c3,c1 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c3,c1 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c3,c1 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c1,c2 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c1,c2 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c1,c2 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c1,c2 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 rows c2 preceding) r1, max(c3) over (partition by c2,c1 order by c1 rows c2 preceding) r2, min(c3) over (partition by c2,c1 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c1 order by c1 range unbounded preceding) r1, max(c3) over (partition by c1 order by c1 range unbounded preceding) r2, min(c3) over (partition by c1 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c1 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c1 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c1 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c1 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c1 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c1,c3 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c1,c3 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c1 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c1 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c3,c1 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c3,c1 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c3,c1 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c3,c1 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows current row) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows current row) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows current row) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows current row) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows current row) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows current row) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c2 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c2 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c2 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c2 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c2 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c2 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c2 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c2 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c2 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c2 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c2 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c2 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c2 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c2 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c2 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c2 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c2 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c2 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range c2 preceding) r0, avg(c3) over (partition by c1 order by c1 range c2 preceding) r1, max(c3) over (partition by c1 order by c1 range c2 preceding) r2, min(c3) over (partition by c1 order by c1 range c2 preceding) r3, avg(c1) over (partition by c1 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c1 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c1 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c1,c2 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c1,c2 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c1,c3 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c1,c3 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c1,c3 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c1,c3 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c1,c3 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c1,c3 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c2,c3 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c2,c3 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c2,c3 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c2,c3 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c2 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c2 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c2 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c2 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c2 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c2 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c3 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c3 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows c2 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows c2 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c2,c1 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c2,c1 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c2,c1 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c2,c1 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c1,c2 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c1,c2 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c2,c1 order by c1 range unbounded preceding) r1, max(c3) over (partition by c2,c1 order by c1 range unbounded preceding) r2, min(c3) over (partition by c2,c1 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c2,c1 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between current row and current row) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between current row and current row) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between current row and current row) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between current row and current row) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c2,c3 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c2,c3 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c2,c3 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c2,c3 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c2,c3 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c2,c3 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c2 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c2 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c2 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c2 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c2 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c2 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c1 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c1 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c1 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c1 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c1 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c1 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows current row) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows current row) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows current row) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows current row) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows current row) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows current row) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range current row) r0, avg(c3) over (partition by c2 order by c1 range current row) r1, max(c3) over (partition by c2 order by c1 range current row) r2, min(c3) over (partition by c2 order by c1 range current row) r3, avg(c1) over (partition by c2 order by c1 range current row) r4, first_value(c1) over (partition by c2 order by c1 range current row) r5, last_value(c1) over (partition by c2 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c1 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c1 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c1 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c1 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c1 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c1 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c3 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c3 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c3 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c3 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c3 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c3 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c1 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c1 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c1 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c1 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c1 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c1 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 rows 1 preceding) r1, max(c3) over (partition by c2,c3 order by c1 rows 1 preceding) r2, min(c3) over (partition by c2,c3 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows current row) r0, avg(c3) over (partition by c2,c3 order by c1 rows current row) r1, max(c3) over (partition by c2,c3 order by c1 rows current row) r2, min(c3) over (partition by c2,c3 order by c1 rows current row) r3, avg(c1) over (partition by c2,c3 order by c1 rows current row) r4, first_value(c1) over (partition by c2,c3 order by c1 rows current row) r5, last_value(c1) over (partition by c2,c3 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c3 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c3 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c3 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c3 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c3 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c3 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c3,c1 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c3,c1 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c3,c1 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c3,c1 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c1,c3 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c1,c3 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c1,c3 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c1,c3 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows current row) r0, avg(c3) over (partition by c3,c1 order by c1 rows current row) r1, max(c3) over (partition by c3,c1 order by c1 rows current row) r2, min(c3) over (partition by c3,c1 order by c1 rows current row) r3, avg(c1) over (partition by c3,c1 order by c1 rows current row) r4, first_value(c1) over (partition by c3,c1 order by c1 rows current row) r5, last_value(c1) over (partition by c3,c1 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c2 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c2 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c2 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c2 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c2 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c2 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c1,c3 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c1,c3 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c1,c3 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c1,c3 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c3 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c3 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c3 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c3 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c3 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c3 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c3,c2 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c3,c2 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c3,c2 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c3,c2 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c1 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c1 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c1 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c1 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c1 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c1 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c2 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c2 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c2 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c2 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c2 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c2 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c3 order by c1 rows between current row and current row) r1, max(c3) over (partition by c3 order by c1 rows between current row and current row) r2, min(c3) over (partition by c3 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c3 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c3 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c3 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows current row) r0, avg(c3) over (partition by c2,c1 order by c1 rows current row) r1, max(c3) over (partition by c2,c1 order by c1 rows current row) r2, min(c3) over (partition by c2,c1 order by c1 rows current row) r3, avg(c1) over (partition by c2,c1 order by c1 rows current row) r4, first_value(c1) over (partition by c2,c1 order by c1 rows current row) r5, last_value(c1) over (partition by c2,c1 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range c2 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 range c2 preceding) r1, max(c3) over (partition by c2,c3 order by c1 range c2 preceding) r2, min(c3) over (partition by c2,c3 order by c1 range c2 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c1,c3 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c1,c3 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c1,c3 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c1,c3 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c1 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c1 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c1 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c1 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c1 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c1 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c3,c2 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c3,c2 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between current row and current row) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between current row and current row) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c3,c1 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c3,c1 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range current row) r0, avg(c3) over (partition by c3,c1 order by c1 range current row) r1, max(c3) over (partition by c3,c1 order by c1 range current row) r2, min(c3) over (partition by c3,c1 order by c1 range current row) r3, avg(c1) over (partition by c3,c1 order by c1 range current row) r4, first_value(c1) over (partition by c3,c1 order by c1 range current row) r5, last_value(c1) over (partition by c3,c1 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c1,c3 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c1,c3 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c3,c2 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c3,c2 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c3,c2 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c3,c2 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c3,c2 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c3,c2 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c2,c1 order by c1 rows between current row and current row) r1, max(c3) over (partition by c2,c1 order by c1 rows between current row and current row) r2, min(c3) over (partition by c2,c1 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c2,c1 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c2,c1 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c2,c1 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c1,c3 order by c1 range unbounded preceding) r1, max(c3) over (partition by c1,c3 order by c1 range unbounded preceding) r2, min(c3) over (partition by c1,c3 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c1,c3 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c3,c2 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c3,c2 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between current row and current row) r0, avg(c3) over (partition by c2 order by c1 range between current row and current row) r1, max(c3) over (partition by c2 order by c1 range between current row and current row) r2, min(c3) over (partition by c2 order by c1 range between current row and current row) r3, avg(c1) over (partition by c2 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c2 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c2 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c2,c1 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c2,c1 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c3,c2 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c3,c2 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c3,c2 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c3,c2 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c1,c2 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c1,c2 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range c2 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 range c2 preceding) r1, max(c3) over (partition by c2,c1 order by c1 range c2 preceding) r2, min(c3) over (partition by c2,c1 order by c1 range c2 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c1 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c3,c1 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c3,c1 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c1 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c3,c2 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c3,c1 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c3,c1 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c3,c1 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c3,c1 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c1,c3 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c1,c3 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c1,c3 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c1,c3 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c2 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c2 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c2 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c2 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c2 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c2 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows c2 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows c2 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c3,c2 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c3,c2 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range current row) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range current row) r1, max(c3) over (partition by c3,c1,c2 order by c1 range current row) r2, min(c3) over (partition by c3,c1,c2 order by c1 range current row) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range current row) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range current row) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c3 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c3 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c3 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c3 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c3 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c3 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c2 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c2 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c2 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c2 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c2 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c2 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c2 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c2 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c3,c1 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c3,c1 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c3,c1 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c3,c1 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c1,c3 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c1,c3 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c2 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c2 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c3,c1 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c3,c1 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c3,c1 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c3,c1 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c3,c2 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c3,c2 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c3,c2 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c3,c2 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c3,c2 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c3,c2 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c2 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c2 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c2 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c2 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c2 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c2 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c2 order by c1 rows 1 preceding) r1, max(c3) over (partition by c2 order by c1 rows 1 preceding) r2, min(c3) over (partition by c2 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c2 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c2 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c2 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c3,c1 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c3,c1 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c3,c2 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c3,c2 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c3,c2 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c3,c2 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c2 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c2 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c2 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c2 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c2 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c2 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c2,c3 order by c1 rows between current row and current row) r1, max(c3) over (partition by c2,c3 order by c1 rows between current row and current row) r2, min(c3) over (partition by c2,c3 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c2,c3 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c1 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c1 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c1 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c1 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c1 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c1 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c2,c1 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c2,c1 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c2,c1 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c2,c1 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c2,c1 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c2,c1 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c3 order by c1 rows 1 preceding) r1, max(c3) over (partition by c3 order by c1 rows 1 preceding) r2, min(c3) over (partition by c3 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c3 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c3 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c3 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c2,c3 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c2,c3 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c3 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c3 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c3 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c3 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c3 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c3 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c3 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c3 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c3 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c3 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c3 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c3 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c2,c1 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c2,c1 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range c2 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range c2 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 range c2 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 range c2 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c2 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c2 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c2 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c2 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c2 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c2 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c3 order by c1 rows c2 preceding) r1, max(c3) over (partition by c3 order by c1 rows c2 preceding) r2, min(c3) over (partition by c3 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c3 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c3 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c3 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c3 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c3 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c3 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c3 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c2,c3 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c2,c3 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c2,c3 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c2,c3 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c2 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c1,c2 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c1,c2 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c2 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c1,c2 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c1,c2 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c1,c2 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c1,c2 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range c2 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 range c2 preceding) r1, max(c3) over (partition by c3,c1 order by c1 range c2 preceding) r2, min(c3) over (partition by c3,c1 order by c1 range c2 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c2,c3 order by c1 range unbounded preceding) r1, max(c3) over (partition by c2,c3 order by c1 range unbounded preceding) r2, min(c3) over (partition by c2,c3 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c2,c3 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range unbounded preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 range unbounded preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c1,c2 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c1,c2 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between current row and current row) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between current row and current row) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c3,c1 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c3,c1 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c3,c1 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c3,c1 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c1,c2 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c1,c2 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c1 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c1 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c1 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c1 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c1 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c1 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c2,c1 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c2,c1 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c2,c1 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c2,c1 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range current row) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range current row) r1, max(c3) over (partition by c1,c3,c2 order by c1 range current row) r2, min(c3) over (partition by c1,c3,c2 order by c1 range current row) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range current row) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range current row) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range current row) r0, avg(c3) over (partition by c3,c2 order by c1 range current row) r1, max(c3) over (partition by c3,c2 order by c1 range current row) r2, min(c3) over (partition by c3,c2 order by c1 range current row) r3, avg(c1) over (partition by c3,c2 order by c1 range current row) r4, first_value(c1) over (partition by c3,c2 order by c1 range current row) r5, last_value(c1) over (partition by c3,c2 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range 1 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 range 1 preceding) r1, max(c3) over (partition by c2,c3 order by c1 range 1 preceding) r2, min(c3) over (partition by c2,c3 order by c1 range 1 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c2 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c2 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c1,c3 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c1,c3 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c1,c3 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c1,c3 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c1 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c1 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c1 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c1 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c1 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c1 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c1 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c1 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c1 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c1 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c1 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c1 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c3 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c3 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c3 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c3 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c3 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c3 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range c2 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 range c2 preceding) r1, max(c3) over (partition by c1,c3 order by c1 range c2 preceding) r2, min(c3) over (partition by c1,c3 order by c1 range c2 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c1 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c1 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c1 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c1 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c1 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c1 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range c2 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range c2 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 range c2 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 range c2 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c3 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c3 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c3 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c3 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c3 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c3 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 rows 1 preceding) r1, max(c3) over (partition by c1,c3 order by c1 rows 1 preceding) r2, min(c3) over (partition by c1,c3 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between current row and current row) r0, avg(c3) over (partition by c2,c1 order by c1 range between current row and current row) r1, max(c3) over (partition by c2,c1 order by c1 range between current row and current row) r2, min(c3) over (partition by c2,c1 order by c1 range between current row and current row) r3, avg(c1) over (partition by c2,c1 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c2,c1 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c2,c1 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c1,c2 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c1,c2 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c3 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c1,c3 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c1,c3 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c3 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between current row and current row) r0, avg(c3) over (partition by c1,c2 order by c1 range between current row and current row) r1, max(c3) over (partition by c1,c2 order by c1 range between current row and current row) r2, min(c3) over (partition by c1,c2 order by c1 range between current row and current row) r3, avg(c1) over (partition by c1,c2 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c1,c2 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c1,c2 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between current row and current row) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between current row and current row) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c2,c3 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c2,c3 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c2,c3 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c2,c3 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range current row) r0, avg(c3) over (partition by c2,c1 order by c1 range current row) r1, max(c3) over (partition by c2,c1 order by c1 range current row) r2, min(c3) over (partition by c2,c1 order by c1 range current row) r3, avg(c1) over (partition by c2,c1 order by c1 range current row) r4, first_value(c1) over (partition by c2,c1 order by c1 range current row) r5, last_value(c1) over (partition by c2,c1 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c1,c2 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c1,c2 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c3 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c3 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c3 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c3 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c3 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c3 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c2 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c2 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c2 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c2 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c2 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c2 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c2 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c2 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c2 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c2 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c2 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c2 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c1,c2 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c1,c2 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c1,c2 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c1,c2 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c2,c1 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c2,c1 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c2,c1 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c2,c1 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c3 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c3 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c3 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c3 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c3 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c3 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c3 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c3 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c2,c1 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c2,c1 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c2,c1 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c2,c1 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c1 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c1 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c1 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c1 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c1 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c1 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c2,c1 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c2,c1 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c2 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c2 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c3 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c3 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c3 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c3 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c3 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c3 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range current row) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range current row) r1, max(c3) over (partition by c2,c3,c1 order by c1 range current row) r2, min(c3) over (partition by c2,c3,c1 order by c1 range current row) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range current row) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range current row) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c2 order by c1 rows between current row and current row) r1, max(c3) over (partition by c2 order by c1 rows between current row and current row) r2, min(c3) over (partition by c2 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c2 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c2 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c2 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c3 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c3 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c3 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c3 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c3 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c3 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c2,c3 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c2,c3 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c2,c3 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c2,c3 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c3,c1 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c3,c1 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c1,c2 order by c1 rows between current row and current row) r1, max(c3) over (partition by c1,c2 order by c1 rows between current row and current row) r2, min(c3) over (partition by c1,c2 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c1,c2 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c1,c2 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c1,c2 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c1,c2 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c1,c2 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c3 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c3 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c3 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c3 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c3 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c3 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c1 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c1 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c1 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c1 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c1 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c1 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c3,c2 order by c1 range unbounded preceding) r1, max(c3) over (partition by c3,c2 order by c1 range unbounded preceding) r2, min(c3) over (partition by c3,c2 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c3,c2 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c3 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c3 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c3 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c3 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c3 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c3 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c2 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c2 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c2 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c2 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c2 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c2 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c1,c3 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c1,c3 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c1,c3 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c1,c3 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c1,c3 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c1,c3 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c1,c3 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c1,c3 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c2,c3 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c2,c3 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c2,c3 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c2,c3 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows 1 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows 1 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c2 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c2 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c2 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c2 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c2 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c2 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c3,c2 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c3,c2 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c3,c2 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c3,c2 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c2 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c2 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c2 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c2 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c2 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c2 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range 1 preceding) r0, avg(c3) over (partition by c2 order by c1 range 1 preceding) r1, max(c3) over (partition by c2 order by c1 range 1 preceding) r2, min(c3) over (partition by c2 order by c1 range 1 preceding) r3, avg(c1) over (partition by c2 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c2 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c2 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c1 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c1 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c1 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c1 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c1 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c1 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c3 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c3 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c3 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c3 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c3 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c3 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c1,c2 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c1,c2 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c1,c2 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c1,c2 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c1,c2 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c1,c2 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c1 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c1 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c1 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c1 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c1 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c1 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c1,c3 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c1,c3 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c1,c2 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c1,c2 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c1,c2 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c1,c2 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows current row) r0, avg(c3) over (partition by c1,c2 order by c1 rows current row) r1, max(c3) over (partition by c1,c2 order by c1 rows current row) r2, min(c3) over (partition by c1,c2 order by c1 rows current row) r3, avg(c1) over (partition by c1,c2 order by c1 rows current row) r4, first_value(c1) over (partition by c1,c2 order by c1 rows current row) r5, last_value(c1) over (partition by c1,c2 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c2,c3 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c2,c3 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c2 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c2 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c2 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c2 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c2 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c2 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c1 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c1 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c1 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c1 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c1 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c1 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c2 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c2 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c2 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c2 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c2 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c2 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c1 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c2,c1 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c2,c1 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c1 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range c2 preceding) r0, avg(c3) over (partition by c3 order by c1 range c2 preceding) r1, max(c3) over (partition by c3 order by c1 range c2 preceding) r2, min(c3) over (partition by c3 order by c1 range c2 preceding) r3, avg(c1) over (partition by c3 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c3 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c3 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range unbounded preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 range unbounded preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c2,c1 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c2,c1 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c2,c1 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c2,c1 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows current row) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows current row) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows current row) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows current row) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows current row) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows current row) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c1 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c1 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c1 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c1 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c1 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c1 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range c2 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range c2 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 range c2 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 range c2 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c2,c1 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c2,c1 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c1,c3 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c1,c3 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows current row) r0, avg(c3) over (partition by c3,c2 order by c1 rows current row) r1, max(c3) over (partition by c3,c2 order by c1 rows current row) r2, min(c3) over (partition by c3,c2 order by c1 rows current row) r3, avg(c1) over (partition by c3,c2 order by c1 rows current row) r4, first_value(c1) over (partition by c3,c2 order by c1 rows current row) r5, last_value(c1) over (partition by c3,c2 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c1,c2 order by c1 range unbounded preceding) r1, max(c3) over (partition by c1,c2 order by c1 range unbounded preceding) r2, min(c3) over (partition by c1,c2 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c1,c2 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range c2 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range c2 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 range c2 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 range c2 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c1 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c1 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c1 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c1 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c1 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c1 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c2,c1 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c2,c1 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c2,c3 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c2,c3 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 rows 1 preceding) r1, max(c3) over (partition by c1,c2 order by c1 rows 1 preceding) r2, min(c3) over (partition by c1,c2 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c2 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c2 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c2 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c2 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c2 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c2 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c3,c1 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c3,c1 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c2 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c2 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c2 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c2 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c3 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c3 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c3 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c3 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c3 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c3 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 rows c2 preceding) r1, max(c3) over (partition by c1,c3 order by c1 rows c2 preceding) r2, min(c3) over (partition by c1,c3 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c1,c3 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c1,c3 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c1 order by c1 rows 1 preceding) r1, max(c3) over (partition by c1 order by c1 rows 1 preceding) r2, min(c3) over (partition by c1 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c1 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c1 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c1 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c2,c1 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c2,c1 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c2,c1 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c2,c1 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c1 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c1 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c1 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c1 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c1 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c1 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c1 order by c1 rows between current row and current row) r1, max(c3) over (partition by c1 order by c1 rows between current row and current row) r2, min(c3) over (partition by c1 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c1 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c1 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c1 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows 1 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows 1 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c3 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c3 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c3 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c3 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c3 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c3 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c1,c2 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c1,c2 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c1,c2 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c1,c2 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range 1 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range 1 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 range 1 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 range 1 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c3,c2 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c3,c2 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c3,c2 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c3,c2 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c1,c2 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c1,c2 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c1,c2 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c1,c2 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range 1 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 range 1 preceding) r1, max(c3) over (partition by c1,c3 order by c1 range 1 preceding) r2, min(c3) over (partition by c1,c3 order by c1 range 1 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c3 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c3 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c3 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c3 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c3 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c3 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c1,c3 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c1,c3 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c1,c3 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c1,c3 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c2,c3 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c2,c3 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c2 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c2 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c2 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c2 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c2 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c2 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c1,c2 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c1,c2 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c1 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c1 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c1 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c1 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c1 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c1 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range unbounded preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 range unbounded preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c2,c1 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c2,c1 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range c2 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 range c2 preceding) r1, max(c3) over (partition by c1,c2 order by c1 range c2 preceding) r2, min(c3) over (partition by c1,c2 order by c1 range c2 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows 1 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows 1 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c3 order by c1 range unbounded preceding) r1, max(c3) over (partition by c3 order by c1 range unbounded preceding) r2, min(c3) over (partition by c3 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c3 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c3 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c3 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c1 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c1 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c1 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c1 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c1 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c1 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c2,c3 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c2,c3 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c2,c3 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c2,c3 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c3 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c3 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c3 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c3 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c3 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c3 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 rows 1 preceding) r1, max(c3) over (partition by c2,c1 order by c1 rows 1 preceding) r2, min(c3) over (partition by c2,c1 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c2,c3 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c2,c3 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c3 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c3 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c1 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c1 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c1 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c1 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c1 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c1 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c1 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c1 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c1 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c1 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c1 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c1 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c2 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c2 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c2 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c2 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c2 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c2 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between current row and current row) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between current row and current row) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between current row and current row) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between current row and current row) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c3 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c3 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c3 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c3 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c3 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c3 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c3 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c3 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c3 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c3 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c3 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c3 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range 1 preceding) r0, avg(c3) over (partition by c1 order by c1 range 1 preceding) r1, max(c3) over (partition by c1 order by c1 range 1 preceding) r2, min(c3) over (partition by c1 order by c1 range 1 preceding) r3, avg(c1) over (partition by c1 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c1 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c1 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c3,c1 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c3,c1 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c2,c1 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c2,c1 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c2,c3 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c2,c3 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c2,c3 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c2,c3 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range c2 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 range c2 preceding) r1, max(c3) over (partition by c3,c2 order by c1 range c2 preceding) r2, min(c3) over (partition by c3,c2 order by c1 range c2 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c3,c2 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c3,c2 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c3,c2 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c3,c2 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c1,c3 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c1,c3 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c3 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c3 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c3 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c3 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c3 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c3 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between current row and current row) r0, avg(c3) over (partition by c1,c3 order by c1 range between current row and current row) r1, max(c3) over (partition by c1,c3 order by c1 range between current row and current row) r2, min(c3) over (partition by c1,c3 order by c1 range between current row and current row) r3, avg(c1) over (partition by c1,c3 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c1,c3 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c1,c3 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range current row) r0, avg(c3) over (partition by c1,c3 order by c1 range current row) r1, max(c3) over (partition by c1,c3 order by c1 range current row) r2, min(c3) over (partition by c1,c3 order by c1 range current row) r3, avg(c1) over (partition by c1,c3 order by c1 range current row) r4, first_value(c1) over (partition by c1,c3 order by c1 range current row) r5, last_value(c1) over (partition by c1,c3 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c1 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c1 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c1 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c1 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c1 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c1 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c3 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c3 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c3 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c3 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c3 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c3 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c1 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c1 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c1 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c1 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c1 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c1 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c2,c3 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c2,c3 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c2,c3 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c2,c3 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c2,c3 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c2,c3 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range 1 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range 1 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 range 1 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 range 1 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c3,c2 order by c1 rows between current row and current row) r1, max(c3) over (partition by c3,c2 order by c1 rows between current row and current row) r2, min(c3) over (partition by c3,c2 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c3,c2 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c1 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c1 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c1 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c1 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c1 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c1 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c2,c3 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c2,c3 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c2 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c2 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c2 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c2 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c2 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c2 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c1 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c1 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c2,c3 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c2,c3 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c3,c1 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c3,c1 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c3,c1 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c3,c1 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 rows 1 preceding) r1, max(c3) over (partition by c3,c2 order by c1 rows 1 preceding) r2, min(c3) over (partition by c3,c2 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c3,c2 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c3,c2 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c3,c2 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c3,c2 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range unbounded preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 range unbounded preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c2,c1 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c2,c1 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c2,c1 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c2,c1 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows 1 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows 1 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c1,c3 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c1,c3 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range 1 preceding) r0, avg(c3) over (partition by c3 order by c1 range 1 preceding) r1, max(c3) over (partition by c3 order by c1 range 1 preceding) r2, min(c3) over (partition by c3 order by c1 range 1 preceding) r3, avg(c1) over (partition by c3 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c3 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c3 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c2 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c2 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c2 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c2 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c2 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c2 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows current row) r0, avg(c3) over (partition by c3 order by c1 rows current row) r1, max(c3) over (partition by c3 order by c1 rows current row) r2, min(c3) over (partition by c3 order by c1 rows current row) r3, avg(c1) over (partition by c3 order by c1 rows current row) r4, first_value(c1) over (partition by c3 order by c1 rows current row) r5, last_value(c1) over (partition by c3 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c3,c2 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c3,c2 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c3,c2 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c3,c2 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c1 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c1 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c3 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c3 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c3 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c3 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c3 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c3 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c3,c1 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c3,c1 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 rows 1 preceding) r1, max(c3) over (partition by c3,c1 order by c1 rows 1 preceding) r2, min(c3) over (partition by c3,c1 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c1 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c1 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c1 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c1 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c1 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c1 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c1,c3 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c1,c3 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c1,c3 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c1,c3 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c2 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c2 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c2 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c2 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c2 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c2 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c3,c1 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c3,c1 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c3,c1 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c3,c1 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows current row) r0, avg(c3) over (partition by c1 order by c1 rows current row) r1, max(c3) over (partition by c1 order by c1 rows current row) r2, min(c3) over (partition by c1 order by c1 rows current row) r3, avg(c1) over (partition by c1 order by c1 rows current row) r4, first_value(c1) over (partition by c1 order by c1 rows current row) r5, last_value(c1) over (partition by c1 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c3 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c3 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c3 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c3 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c3 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c3 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c1 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c1 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c1 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c1 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c1 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c1 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c2,c1 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c2 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c2 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c2 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c2 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c2 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c2 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c2 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c2 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c2 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c2 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c2 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c2 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows c2 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows c2 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows current row) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows current row) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows current row) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows current row) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows current row) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows current row) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c1 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c1 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c1 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c1 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c1 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c1 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c3 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c3 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c3 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c3 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c3 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c3 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c2,c3 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c2,c3 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range current row) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range current row) r1, max(c3) over (partition by c2,c1,c3 order by c1 range current row) r2, min(c3) over (partition by c2,c1,c3 order by c1 range current row) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range current row) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range current row) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range current row) r0, avg(c3) over (partition by c1 order by c1 range current row) r1, max(c3) over (partition by c1 order by c1 range current row) r2, min(c3) over (partition by c1 order by c1 range current row) r3, avg(c1) over (partition by c1 order by c1 range current row) r4, first_value(c1) over (partition by c1 order by c1 range current row) r5, last_value(c1) over (partition by c1 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c3 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c3 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c3 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c3 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c3 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c3 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between current row and current row) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between current row and current row) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between current row and current row) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between current row and current row) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between current row and current row) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between current row and current row) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range unbounded preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 range unbounded preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between current row and current row) r0, avg(c3) over (partition by c3,c2 order by c1 range between current row and current row) r1, max(c3) over (partition by c3,c2 order by c1 range between current row and current row) r2, min(c3) over (partition by c3,c2 order by c1 range between current row and current row) r3, avg(c1) over (partition by c3,c2 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c3,c2 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c3,c2 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c2 order by c1 rows c2 preceding) r1, max(c3) over (partition by c2 order by c1 rows c2 preceding) r2, min(c3) over (partition by c2 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c2 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c2 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c2 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c3,c1 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c3,c1 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c3,c1 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c3,c1 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c2,c3 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c2,c3 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c2,c3 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c2,c3 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c1,c2 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c1,c2 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c1,c2 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c1,c2 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c3,c1 order by c1 range unbounded preceding) r1, max(c3) over (partition by c3,c1 order by c1 range unbounded preceding) r2, min(c3) over (partition by c3,c1 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c3,c1 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 rows c2 preceding) r1, max(c3) over (partition by c2,c3 order by c1 rows c2 preceding) r2, min(c3) over (partition by c2,c3 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between current row and current row) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between current row and current row) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between current row and current row) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between current row and current row) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range 1 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 range 1 preceding) r1, max(c3) over (partition by c3,c1 order by c1 range 1 preceding) r2, min(c3) over (partition by c3,c1 order by c1 range 1 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c3,c1 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c3,c1 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c2,c1 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c2,c1 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c2,c1 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c2,c1 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c2 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c2 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c2 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c2 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c2 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c2 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c1,c3 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c1,c3 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c3,c2 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c3,c2 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c3,c2 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c3,c2 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c3,c2 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c3,c2 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c3 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c3 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c3 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c3 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c3 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c3 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c2,c3 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c2,c3 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c2,c3 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c2,c3 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c3,c1 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c3,c1 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c2 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c2 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c2 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c2 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c2 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c2 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c1 order by c1 rows c2 preceding) r1, max(c3) over (partition by c1 order by c1 rows c2 preceding) r2, min(c3) over (partition by c1 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c1 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c1 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c1 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range 1 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range 1 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 range 1 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 range 1 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c2 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c2 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c2 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c2 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c2 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c2 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c3,c2 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c3,c2 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c2 order by c1 range between 1 following and c5 + 100 following) r1, max(c3) over (partition by c3,c2 order by c1 range between 1 following and c5 + 100 following) r2, min(c3) over (partition by c3,c2 order by c1 range between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c2 order by c1 range between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c1 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c1 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c1 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c1 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c1 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c1 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c1,c3 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c1,c3 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c1,c3 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c1,c3 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c1,c3 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c1,c3 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c3,c1 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c3,c1 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c3,c1 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c3,c1 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range unbounded preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range unbounded preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 range unbounded preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 range unbounded preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range unbounded preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range unbounded preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c2 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c2 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c2 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c2 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c2 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c2 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c3,c2 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c3,c2 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c3,c2 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c3,c2 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c2,c3 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c2,c3 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c2,c3 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c2,c3 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c2,c3 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c2,c3 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c3 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c3 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c3 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c3 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c3 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c3 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between current row and current row) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between current row and current row) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c1 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c1 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c1 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c1 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c1 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c1 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows 1 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows 1 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c3 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c3 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c3 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c3 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c3 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c3 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between c2 preceding and unbounded following) r0, avg(c3) over (partition by c2 order by c1 rows between c2 preceding and unbounded following) r1, max(c3) over (partition by c2 order by c1 rows between c2 preceding and unbounded following) r2, min(c3) over (partition by c2 order by c1 rows between c2 preceding and unbounded following) r3, avg(c1) over (partition by c2 order by c1 rows between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c2 order by c1 rows between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c2 order by c1 rows between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c2 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c2 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c2 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c2 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c2 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c2 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows current row) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows current row) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows current row) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows current row) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows current row) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows current row) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range current row) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range current row) r1, max(c3) over (partition by c3,c2,c1 order by c1 range current row) r2, min(c3) over (partition by c3,c2,c1 order by c1 range current row) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range current row) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range current row) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c3,c2 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c3,c2 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c2 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c2 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c2 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c2 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c2 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c2 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c1,c2 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between current row and 1 following) r0, avg(c3) over (partition by c1 order by c1 rows between current row and 1 following) r1, max(c3) over (partition by c1 order by c1 rows between current row and 1 following) r2, min(c3) over (partition by c1 order by c1 rows between current row and 1 following) r3, avg(c1) over (partition by c1 order by c1 rows between current row and 1 following) r4, first_value(c1) over (partition by c1 order by c1 rows between current row and 1 following) r5, last_value(c1) over (partition by c1 order by c1 rows between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c1 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c1 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c1 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c1 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c1 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c1 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c2,c3 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c2,c3 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c1 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c1 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c1 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c1 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c1 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c1 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c2,c3 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c1 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c1 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c1 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c1 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c1 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c1 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows unbounded preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows unbounded preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows unbounded preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows unbounded preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows unbounded preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows unbounded preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows unbounded preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c3 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c3 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c3 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c3 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c3 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c3 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c3,c1 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c3,c1 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c3,c1 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c3,c1 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c3 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c3 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c3 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c3 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c3 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c3 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c1 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c1 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c1 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c1 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c1,c3 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c1,c3 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c1,c3 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c1,c3 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c1,c3 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c1,c3 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c1 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c1 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c1 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c1 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c1 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c1 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c2,c3 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c2 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c2 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c2 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c2 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c2 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c2 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between current row and c5 + 100 following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between current row and c5 + 100 following) r1, max(c3) over (partition by c2,c1 order by c1 rows between current row and c5 + 100 following) r2, min(c3) over (partition by c2,c1 order by c1 rows between current row and c5 + 100 following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between current row and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between current row and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c2 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c2 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range current row) r0, avg(c3) over (partition by c3 order by c1 range current row) r1, max(c3) over (partition by c3 order by c1 range current row) r2, min(c3) over (partition by c3 order by c1 range current row) r3, avg(c1) over (partition by c3 order by c1 range current row) r4, first_value(c1) over (partition by c3 order by c1 range current row) r5, last_value(c1) over (partition by c3 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c3,c1 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c3,c1 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and c2 preceding) r1, max(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and c2 preceding) r2, min(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c1,c3 order by c1 rows between current row and current row) r1, max(c3) over (partition by c1,c3 order by c1 rows between current row and current row) r2, min(c3) over (partition by c1,c3 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c1,c3 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows c2 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows c2 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between 1 following and unbounded following) r0, avg(c3) over (partition by c1 order by c1 range between 1 following and unbounded following) r1, max(c3) over (partition by c1 order by c1 range between 1 following and unbounded following) r2, min(c3) over (partition by c1 order by c1 range between 1 following and unbounded following) r3, avg(c1) over (partition by c1 order by c1 range between 1 following and unbounded following) r4, first_value(c1) over (partition by c1 order by c1 range between 1 following and unbounded following) r5, last_value(c1) over (partition by c1 order by c1 range between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c3,c2 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c3,c2 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range c2 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range c2 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 range c2 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 range c2 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range 1 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range 1 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 range 1 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 range 1 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c1 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c1 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c1 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c1 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c1 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c1 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and current row) r0, avg(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and current row) r1, max(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and current row) r2, min(c3) over (partition by c3,c2 order by c1 range between unbounded preceding and current row) r3, avg(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and current row) r4, first_value(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and current row) r5, last_value(c1) over (partition by c3,c2 order by c1 range between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range current row) r0, avg(c3) over (partition by c2,c3 order by c1 range current row) r1, max(c3) over (partition by c2,c3 order by c1 range current row) r2, min(c3) over (partition by c2,c3 order by c1 range current row) r3, avg(c1) over (partition by c2,c3 order by c1 range current row) r4, first_value(c1) over (partition by c2,c3 order by c1 range current row) r5, last_value(c1) over (partition by c2,c3 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c3 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c3 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c3 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c3 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c3 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c3 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between c2 preceding and 1 following) r0, avg(c3) over (partition by c2 order by c1 range between c2 preceding and 1 following) r1, max(c3) over (partition by c2 order by c1 range between c2 preceding and 1 following) r2, min(c3) over (partition by c2 order by c1 range between c2 preceding and 1 following) r3, avg(c1) over (partition by c2 order by c1 range between c2 preceding and 1 following) r4, first_value(c1) over (partition by c2 order by c1 range between c2 preceding and 1 following) r5, last_value(c1) over (partition by c2 order by c1 range between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c3,c1 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows current row) r0, avg(c3) over (partition by c1,c3 order by c1 rows current row) r1, max(c3) over (partition by c1,c3 order by c1 rows current row) r2, min(c3) over (partition by c1,c3 order by c1 rows current row) r3, avg(c1) over (partition by c1,c3 order by c1 rows current row) r4, first_value(c1) over (partition by c1,c3 order by c1 rows current row) r5, last_value(c1) over (partition by c1,c3 order by c1 rows current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 following) r0, avg(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 following) r1, max(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 following) r2, min(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 following) r3, avg(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c1 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c1 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c1 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c1 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c1 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c1 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c2,c1 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c2,c1 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c2,c1 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c2,c1 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between 1 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between 1 following and c5 + 100 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between 1 following and c5 + 100 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between 1 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between 1 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between 1 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between current row and unbounded following) r0, avg(c3) over (partition by c2,c3 order by c1 range between current row and unbounded following) r1, max(c3) over (partition by c2,c3 order by c1 range between current row and unbounded following) r2, min(c3) over (partition by c2,c3 order by c1 range between current row and unbounded following) r3, avg(c1) over (partition by c2,c3 order by c1 range between current row and unbounded following) r4, first_value(c1) over (partition by c2,c3 order by c1 range between current row and unbounded following) r5, last_value(c1) over (partition by c2,c3 order by c1 range between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c3 order by c1 range between 1 preceding and 1 preceding) r1, max(c3) over (partition by c3 order by c1 range between 1 preceding and 1 preceding) r2, min(c3) over (partition by c3 order by c1 range between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c3 order by c1 range between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c3 order by c1 range between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c3 order by c1 range between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c2,c3 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c3 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and unbounded following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and unbounded following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and unbounded following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between 1 preceding and unbounded following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c3 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c3 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c3 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c3 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c3 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c3 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c2,c1 order by c1 range between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 range between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c1,c2 order by c1 range between c5 + 100 following and 1 following) r1, max(c3) over (partition by c1,c2 order by c1 range between c5 + 100 following and 1 following) r2, min(c3) over (partition by c1,c2 order by c1 range between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c1,c2 order by c1 range between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c1 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c1 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c1 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c1 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c1 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c1 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c3 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c3 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c3 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c3 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c3 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c3 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c3,c2 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c3,c2 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c3,c2 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c3,c2 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c3,c2 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c3,c2 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range 1 preceding) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range 1 preceding) r1, max(c3) over (partition by c3,c1,c2 order by c1 range 1 preceding) r2, min(c3) over (partition by c3,c1,c2 order by c1 range 1 preceding) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between current row and c5 + 100 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between current row and c5 + 100 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between current row and c5 + 100 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between current row and c5 + 100 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between current row and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between current row and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between current row and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c2 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c2 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c2 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c2 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c2 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c2 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between unbounded preceding and 1 preceding) r0, avg(c3) over (partition by c2 order by c1 rows between unbounded preceding and 1 preceding) r1, max(c3) over (partition by c2 order by c1 rows between unbounded preceding and 1 preceding) r2, min(c3) over (partition by c2 order by c1 rows between unbounded preceding and 1 preceding) r3, avg(c1) over (partition by c2 order by c1 rows between unbounded preceding and 1 preceding) r4, first_value(c1) over (partition by c2 order by c1 rows between unbounded preceding and 1 preceding) r5, last_value(c1) over (partition by c2 order by c1 rows between unbounded preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c1 order by c1 range between unbounded preceding and unbounded following) r1, max(c3) over (partition by c1 order by c1 range between unbounded preceding and unbounded following) r2, min(c3) over (partition by c1 order by c1 range between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c1 order by c1 range between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c1 order by c1 range between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c1 order by c1 range between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c3 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c3 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c3 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c3 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c3 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c3 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between 1 preceding and current row) r0, avg(c3) over (partition by c1 order by c1 rows between 1 preceding and current row) r1, max(c3) over (partition by c1 order by c1 rows between 1 preceding and current row) r2, min(c3) over (partition by c1 order by c1 rows between 1 preceding and current row) r3, avg(c1) over (partition by c1 order by c1 rows between 1 preceding and current row) r4, first_value(c1) over (partition by c1 order by c1 rows between 1 preceding and current row) r5, last_value(c1) over (partition by c1 order by c1 rows between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and unbounded following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and unbounded following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and unbounded following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between c2 preceding and unbounded following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and unbounded following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and unbounded following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between c2 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and current row) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and current row) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and current row) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and current row) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and current row) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and current row) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between 1 following and 1 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between 1 following and 1 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between 1 following and 1 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between 1 following and 1 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between 1 following and 1 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 following and 1 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c3,c1 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c3,c1 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c3,c1 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c3,c1 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c3,c1 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c3,c1 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c3 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c3 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c3 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c3 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c3 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c3 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 range between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 following) r0, avg(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 following) r1, max(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 following) r2, min(c3) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 following) r3, avg(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 following) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 following) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between c2 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c2,c1 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c2,c1 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c1 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c1 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c1 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c1 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c1 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c1 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and unbounded following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and unbounded following) r1, max(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and unbounded following) r2, min(c3) over (partition by c1,c2 order by c1 rows between 1 preceding and unbounded following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and unbounded following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and unbounded following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between 1 preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range current row) r0, avg(c3) over (partition by c1,c2 order by c1 range current row) r1, max(c3) over (partition by c1,c2 order by c1 range current row) r2, min(c3) over (partition by c1,c2 order by c1 range current row) r3, avg(c1) over (partition by c1,c2 order by c1 range current row) r4, first_value(c1) over (partition by c1,c2 order by c1 range current row) r5, last_value(c1) over (partition by c1,c2 order by c1 range current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between current row and current row) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between current row and current row) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between current row and current row) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between current row and current row) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r1, max(c3) over (partition by c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r2, min(c3) over (partition by c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between c5 + 100 following and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between c5 + 100 following and unbounded following) r0, avg(c3) over (partition by c3 order by c1 range between c5 + 100 following and unbounded following) r1, max(c3) over (partition by c3 order by c1 range between c5 + 100 following and unbounded following) r2, min(c3) over (partition by c3 order by c1 range between c5 + 100 following and unbounded following) r3, avg(c1) over (partition by c3 order by c1 range between c5 + 100 following and unbounded following) r4, first_value(c1) over (partition by c3 order by c1 range between c5 + 100 following and unbounded following) r5, last_value(c1) over (partition by c3 order by c1 range between c5 + 100 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between current row and current row) r0, avg(c3) over (partition by c3,c1 order by c1 rows between current row and current row) r1, max(c3) over (partition by c3,c1 order by c1 rows between current row and current row) r2, min(c3) over (partition by c3,c1 order by c1 rows between current row and current row) r3, avg(c1) over (partition by c3,c1 order by c1 rows between current row and current row) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between current row and current row) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c3,c2 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c3,c2 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c3,c2 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c3,c2 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c3,c2 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c3,c2 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 range c2 preceding) r0, avg(c3) over (partition by c2 order by c1 range c2 preceding) r1, max(c3) over (partition by c2 order by c1 range c2 preceding) r2, min(c3) over (partition by c2 order by c1 range c2 preceding) r3, avg(c1) over (partition by c2 order by c1 range c2 preceding) r4, first_value(c1) over (partition by c2 order by c1 range c2 preceding) r5, last_value(c1) over (partition by c2 order by c1 range c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 following) r0, avg(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 following) r1, max(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 following) r2, min(c3) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 following) r3, avg(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 following) r4, first_value(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 following) r5, last_value(c1) over (partition by c1,c2 order by c1 rows between unbounded preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows 1 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows 1 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows 1 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows 1 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows 1 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows 1 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 range 1 preceding) r0, avg(c3) over (partition by c3,c2 order by c1 range 1 preceding) r1, max(c3) over (partition by c3,c2 order by c1 range 1 preceding) r2, min(c3) over (partition by c3,c2 order by c1 range 1 preceding) r3, avg(c1) over (partition by c3,c2 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c3,c2 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c3,c2 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c3 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c3 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c3 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c3 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c3 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c3 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2 order by c1 range between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between c2 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range between current row and 1 following) r0, avg(c3) over (partition by c2,c1 order by c1 range between current row and 1 following) r1, max(c3) over (partition by c2,c1 order by c1 range between current row and 1 following) r2, min(c3) over (partition by c2,c1 order by c1 range between current row and 1 following) r3, avg(c1) over (partition by c2,c1 order by c1 range between current row and 1 following) r4, first_value(c1) over (partition by c2,c1 order by c1 range between current row and 1 following) r5, last_value(c1) over (partition by c2,c1 order by c1 range between current row and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3 order by c1 range between current row and current row) r0, avg(c3) over (partition by c2,c3 order by c1 range between current row and current row) r1, max(c3) over (partition by c2,c3 order by c1 range between current row and current row) r2, min(c3) over (partition by c2,c3 order by c1 range between current row and current row) r3, avg(c1) over (partition by c2,c3 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c2,c3 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c2,c3 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 range between c2 preceding and c2 preceding) r0, avg(c3) over (partition by c1 order by c1 range between c2 preceding and c2 preceding) r1, max(c3) over (partition by c1 order by c1 range between c2 preceding and c2 preceding) r2, min(c3) over (partition by c1 order by c1 range between c2 preceding and c2 preceding) r3, avg(c1) over (partition by c1 order by c1 range between c2 preceding and c2 preceding) r4, first_value(c1) over (partition by c1 order by c1 range between c2 preceding and c2 preceding) r5, last_value(c1) over (partition by c1 order by c1 range between c2 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range between 1 preceding and current row) r0, avg(c3) over (partition by c1,c2 order by c1 range between 1 preceding and current row) r1, max(c3) over (partition by c1,c2 order by c1 range between 1 preceding and current row) r2, min(c3) over (partition by c1,c2 order by c1 range between 1 preceding and current row) r3, avg(c1) over (partition by c1,c2 order by c1 range between 1 preceding and current row) r4, first_value(c1) over (partition by c1,c2 order by c1 range between 1 preceding and current row) r5, last_value(c1) over (partition by c1,c2 order by c1 range between 1 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between 1 following and 1 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between 1 following and 1 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between 1 following and 1 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between 1 following and 1 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between 1 following and 1 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 following and 1 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c2 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c2 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c2 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c2 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c2 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c2 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 range between current row and current row) r0, avg(c3) over (partition by c3,c2,c1 order by c1 range between current row and current row) r1, max(c3) over (partition by c3,c2,c1 order by c1 range between current row and current row) r2, min(c3) over (partition by c3,c2,c1 order by c1 range between current row and current row) r3, avg(c1) over (partition by c3,c2,c1 order by c1 range between current row and current row) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 range between current row and current row) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 range between current row and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between c5 + 100 following and 1 following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between c5 + 100 following and 1 following) r1, max(c3) over (partition by c3,c1 order by c1 rows between c5 + 100 following and 1 following) r2, min(c3) over (partition by c3,c1 order by c1 rows between c5 + 100 following and 1 following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between c5 + 100 following and 1 following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between c5 + 100 following and 1 following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between c5 + 100 following and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c3 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c3 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c3 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c3 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c3 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c3 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows between 1 preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c3,c2 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c2 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c2 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c2 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c2 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c2 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c2 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 range between 1 preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows between unbounded preceding and c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 preceding) r1, max(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 preceding) r2, min(c3) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between c2 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 rows between current row and unbounded following) r0, avg(c3) over (partition by c2,c1 order by c1 rows between current row and unbounded following) r1, max(c3) over (partition by c2,c1 order by c1 rows between current row and unbounded following) r2, min(c3) over (partition by c2,c1 order by c1 rows between current row and unbounded following) r3, avg(c1) over (partition by c2,c1 order by c1 rows between current row and unbounded following) r4, first_value(c1) over (partition by c2,c1 order by c1 rows between current row and unbounded following) r5, last_value(c1) over (partition by c2,c1 order by c1 rows between current row and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 preceding) r0, avg(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 preceding) r1, max(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 preceding) r2, min(c3) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 preceding) r3, avg(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 preceding) r4, first_value(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 preceding) r5, last_value(c1) over (partition by c1,c3 order by c1 rows between 1 preceding and 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and unbounded following) r0, avg(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and unbounded following) r1, max(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and unbounded following) r2, min(c3) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and unbounded following) r3, avg(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and unbounded following) r4, first_value(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and unbounded following) r5, last_value(c1) over (partition by c3,c2,c1 order by c1 rows between unbounded preceding and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2,c3 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c1,c2,c3 order by c1 rows c2 preceding) r1, max(c3) over (partition by c1,c2,c3 order by c1 rows c2 preceding) r2, min(c3) over (partition by c1,c2,c3 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c1,c2,c3 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c1,c2,c3 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c1,c2,c3 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c2 order by c1 range 1 preceding) r0, avg(c3) over (partition by c1,c2 order by c1 range 1 preceding) r1, max(c3) over (partition by c1,c2 order by c1 range 1 preceding) r2, min(c3) over (partition by c1,c2 order by c1 range 1 preceding) r3, avg(c1) over (partition by c1,c2 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c1,c2 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c1,c2 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1 order by c1 rows between 1 following and unbounded following) r0, avg(c3) over (partition by c1 order by c1 rows between 1 following and unbounded following) r1, max(c3) over (partition by c1 order by c1 rows between 1 following and unbounded following) r2, min(c3) over (partition by c1 order by c1 rows between 1 following and unbounded following) r3, avg(c1) over (partition by c1 order by c1 rows between 1 following and unbounded following) r4, first_value(c1) over (partition by c1 order by c1 rows between 1 following and unbounded following) r5, last_value(c1) over (partition by c1 order by c1 rows between 1 following and unbounded following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c3,c1 order by c1 rows between unbounded preceding and c5 + 100 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c3,c1 order by c1 rows c2 preceding) r0, avg(c3) over (partition by c2,c3,c1 order by c1 rows c2 preceding) r1, max(c3) over (partition by c2,c3,c1 order by c1 rows c2 preceding) r2, min(c3) over (partition by c2,c3,c1 order by c1 rows c2 preceding) r3, avg(c1) over (partition by c2,c3,c1 order by c1 rows c2 preceding) r4, first_value(c1) over (partition by c2,c3,c1 order by c1 rows c2 preceding) r5, last_value(c1) over (partition by c2,c3,c1 order by c1 rows c2 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1 order by c1 range 1 preceding) r0, avg(c3) over (partition by c2,c1 order by c1 range 1 preceding) r1, max(c3) over (partition by c2,c1 order by c1 range 1 preceding) r2, min(c3) over (partition by c2,c1 order by c1 range 1 preceding) r3, avg(c1) over (partition by c2,c1 order by c1 range 1 preceding) r4, first_value(c1) over (partition by c2,c1 order by c1 range 1 preceding) r5, last_value(c1) over (partition by c2,c1 order by c1 range 1 preceding) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and current row) r0, avg(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and current row) r1, max(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and current row) r2, min(c3) over (partition by c3,c1,c2 order by c1 range between c2 preceding and current row) r3, avg(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and current row) r4, first_value(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and current row) r5, last_value(c1) over (partition by c3,c1,c2 order by c1 range between c2 preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2 order by c1 rows between unbounded preceding and current row) r0, avg(c3) over (partition by c2 order by c1 rows between unbounded preceding and current row) r1, max(c3) over (partition by c2 order by c1 rows between unbounded preceding and current row) r2, min(c3) over (partition by c2 order by c1 rows between unbounded preceding and current row) r3, avg(c1) over (partition by c2 order by c1 rows between unbounded preceding and current row) r4, first_value(c1) over (partition by c2 order by c1 rows between unbounded preceding and current row) r5, last_value(c1) over (partition by c2 order by c1 rows between unbounded preceding and current row) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 following) r0, avg(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 following) r1, max(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 following) r2, min(c3) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 following) r3, avg(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 following) r4, first_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 following) r5, last_value(c1) over (partition by c2,c1,c3 order by c1 rows between 1 preceding and 1 following) r6 from test order by c1;
select c1,c2,c3,c4, sum(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r0, avg(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r1, max(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r2, min(c3) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r3, avg(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r4, first_value(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r5, last_value(c1) over (partition by c1,c3,c2 order by c1 range between unbounded preceding and c5 + 100 following) r6 from test order by c1;

--enable_abort_on_error
