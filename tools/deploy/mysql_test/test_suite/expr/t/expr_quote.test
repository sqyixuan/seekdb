--disable_query_log
set @@session.explicit_defaults_for_timestamp=off;
--enable_query_log
# owner: dachuan.sdc
# owner group: SQL2
# Test of functions quote

select quote('abc');
select quote('人123');
select quote(null);
select quote(cast(char(183) as char(100))) from dual;