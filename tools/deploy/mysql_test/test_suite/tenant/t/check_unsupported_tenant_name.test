--disable_query_log
set @@session.explicit_defaults_for_timestamp=off;
--enable_query_log
# owner      : gengfu.zpc
# owner group: VOS
# description: check for unsupported tenant names
#--disable_abort_on_error


connect(my_sys,$OBMYSQL_MS0,root@sys,,,$OBMYSQL_PORT);

--echo no need to test in seekdb

disconnect my_sys;
connection default;
